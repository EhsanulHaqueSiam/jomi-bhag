---
phase: 08-persistence-and-scenarios
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/App.tsx
  - src/components/layout/AppLayout.tsx
  - src/components/scenarios/ScenariosPage.tsx
  - src/components/scenarios/ScenarioCard.tsx
  - src/components/scenarios/ComparisonView.tsx
  - src/components/scenarios/EmptyState.tsx
  - src/components/__tests__/scenarios.test.tsx
autonomous: false
requirements:
  - PRST-01
  - PRST-02
  - PRST-03

must_haves:
  truths:
    - "User can navigate to 'My Scenarios' page from top nav (desktop) or bottom nav (mobile)"
    - "User sees a list of saved scenarios as compact cards with name, date, heir summary, and estate value"
    - "User can save current calculation from the scenarios page"
    - "User can rename a scenario inline"
    - "User can delete a scenario with confirmation"
    - "User can clear all scenarios with a stronger confirmation"
    - "User can duplicate a scenario to create a 'what if' copy"
    - "User can load a saved scenario (with unsaved changes warning if applicable)"
    - "User can select exactly 2 scenarios via checkboxes and click Compare"
    - "Comparison view shows heir shares, estate totals, and adjustments side by side"
    - "Values that differ between scenarios are highlighted with pale amber background"
    - "User can start a new calculation from the scenarios page"
    - "Empty state shown when no scenarios are saved"
  artifacts:
    - path: "src/App.tsx"
      provides: "Page-level view switching (wizard vs scenarios)"
      contains: "AppPage"
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Navigation with My Scenarios link in header and mobile bottom bar"
      contains: "My Scenarios"
    - path: "src/components/scenarios/ScenariosPage.tsx"
      provides: "Main scenarios list page with save, compare, and new calculation controls"
      exports: ["ScenariosPage"]
    - path: "src/components/scenarios/ScenarioCard.tsx"
      provides: "Compact scenario card with name, date, heir summary, estate value, and action buttons"
      exports: ["ScenarioCard"]
    - path: "src/components/scenarios/ComparisonView.tsx"
      provides: "Side-by-side comparison table for exactly 2 scenarios with diff highlighting"
      exports: ["ComparisonView"]
    - path: "src/components/scenarios/EmptyState.tsx"
      provides: "Empty state when no scenarios saved"
      exports: ["EmptyState"]
  key_links:
    - from: "src/App.tsx"
      to: "src/components/scenarios/ScenariosPage.tsx"
      via: "conditional render based on AppPage state"
      pattern: "page === 'scenarios'"
    - from: "src/components/layout/AppLayout.tsx"
      to: "src/App.tsx"
      via: "onNavigate callback to switch AppPage"
      pattern: "onNavigate\\('scenarios'\\)"
    - from: "src/components/scenarios/ScenariosPage.tsx"
      to: "src/stores/scenariosStore.ts"
      via: "useScenariosStore hook for CRUD operations"
      pattern: "useScenariosStore"
    - from: "src/components/scenarios/ComparisonView.tsx"
      to: "src/stores/scenariosStore.ts"
      via: "reads selected scenarios for comparison"
      pattern: "useScenariosStore"
    - from: "src/components/scenarios/ScenariosPage.tsx"
      to: "src/stores/wizardStore.ts"
      via: "setState to load scenario, getState for save"
      pattern: "useWizardStore"
---

<objective>
Build the scenarios UI: page navigation, scenario list with compact cards, save/load/duplicate/delete/rename operations, and side-by-side comparison view with diff highlighting.

Purpose: PRST-01 (visible persistence controls), PRST-02 (scenario comparison), PRST-03 (load/modify UI) require the full scenarios page and comparison view.
Output: Navigable "My Scenarios" page with full scenario management and comparison, integrated into App shell with nav links.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-persistence-and-scenarios/08-RESEARCH.md
@.planning/phases/08-persistence-and-scenarios/08-CONTEXT.md
@.planning/phases/08-persistence-and-scenarios/08-01-SUMMARY.md

@src/App.tsx
@src/components/layout/AppLayout.tsx
@src/types/scenario.ts
@src/stores/scenariosStore.ts
@src/stores/wizardStore.ts

<interfaces>
<!-- Types created by Plan 01 that this plan depends on -->

From src/types/scenario.ts (created in Plan 01):
```typescript
export interface ScenarioSummary {
  heirSummary: string
  totalEstateValue: number
  adjustment: 'none' | 'awl' | 'radd'
  heirCount: number
}

export interface Scenario {
  id: string
  name: string
  createdAt: string        // ISO 8601
  updatedAt: string        // ISO 8601
  state: WizardState       // full wizard data snapshot
  summary: ScenarioSummary
}

export type AppPage = 'wizard' | 'scenarios'
```

From src/stores/scenariosStore.ts (created in Plan 01):
```typescript
export const MAX_SCENARIOS: number
export function generateScenarioName(state: WizardState): string
export function computeStateFingerprint(state: WizardState): string

// Store state: scenarios, selectedIds, isComparing, lastSavedHash
// Store actions: saveScenario, loadScenario, deleteScenario, clearAll,
//   duplicateScenario, renameScenario, toggleSelected, startCompare,
//   stopCompare, hasUnsavedChanges, updateLastSavedHash
export const useScenariosStore: ...
```

From src/components/layout/AppLayout.tsx (current):
```typescript
interface AppLayoutProps {
  children: ReactNode
}
export function AppLayout({ children }: AppLayoutProps)
```

From src/App.tsx (current):
```typescript
function App() {
  return (
    <AppLayout>
      <WizardShell />
    </AppLayout>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App routing, AppLayout navigation, ScenariosPage, ScenarioCard, EmptyState</name>
  <files>src/App.tsx, src/components/layout/AppLayout.tsx, src/components/scenarios/ScenariosPage.tsx, src/components/scenarios/ScenarioCard.tsx, src/components/scenarios/EmptyState.tsx</files>
  <action>
    1. Modify `src/App.tsx`:
       - Add `useState<AppPage>('wizard')` for page switching
       - Import `AppPage` from `@/types/scenario`
       - Import `ScenariosPage` from `@/components/scenarios/ScenariosPage`
       - Import `AnimatePresence`, `motion` from `motion/react`
       - Pass `page` and `onNavigate={setPage}` to AppLayout
       - Render conditionally: wizard page shows WizardShell, scenarios page shows ScenariosPage
       - Wrap in `AnimatePresence mode="wait"` with `motion.div` for page transition (simple fade, key on page name)
       - Pass `onNavigate` to ScenariosPage so it can switch back to wizard

    2. Modify `src/components/layout/AppLayout.tsx`:
       - Extend `AppLayoutProps` with `page: AppPage` and `onNavigate: (page: AppPage) => void`
       - Add navigation link in the header area (right side or below subtitle): "My Scenarios" button/link
       - When `page === 'scenarios'`, show "My Scenarios" as active (emerald underline/highlight)
       - When `page === 'wizard'`, show "Calculator" as active
       - Add mobile bottom nav bar with two icons: Calculator (grid/calculator icon) and My Scenarios (folder/bookmark icon)
       - Mobile bottom nav uses the same fixed bottom bar pattern (`fixed bottom-0`) already established in the app
       - Style nav items with TailwindCSS: emerald for active, gray-500 for inactive, hover states
       - Use inline SVG icons (no icon library) -- simple path-based icons for calculator and folder

    3. Create `src/components/scenarios/EmptyState.tsx`:
       - Centered illustration area with a subtle folder/document icon (inline SVG, gray-300)
       - Heading: "No saved scenarios"
       - Subtext: "Save your current calculation to compare different inheritance scenarios"
       - "+ New Calculation" button (emerald, outline style) that calls onNewCalculation prop
       - Props: `{ onNewCalculation: () => void }`

    4. Create `src/components/scenarios/ScenarioCard.tsx`:
       - Props: `{ scenario: Scenario, isSelected: boolean, onToggleSelect: () => void, onLoad: () => void, onDuplicate: () => void, onDelete: () => void, onRename: (name: string) => void }`
       - Card layout: rounded border, subtle shadow, padding
       - Left side: checkbox for comparison selection
       - Top: scenario name (click to edit -- inline contentEditable or input toggle with pencil icon)
       - Below name: date (formatted as "Mar 13, 2026"), heir summary text from `scenario.summary.heirSummary`
       - Below summary: total estate value formatted as BDT with `Intl.NumberFormat('en-IN', { style: 'currency', currency: 'BDT', currencyDisplay: 'narrowSymbol' })`
       - If adjustment is 'awl' or 'radd', show a small badge (amber for Awl, blue for Radd) matching Phase 3 pattern
       - Action buttons row at bottom: Load (emerald text), Duplicate (gray text), Delete (red text)
       - Delete button shows inline confirmation: "Delete this scenario?" with Confirm/Cancel buttons
       - Inline rename: click name -> shows input field, blur or Enter saves, Escape cancels

    5. Create `src/components/scenarios/ScenariosPage.tsx`:
       - Props: `{ onNavigate: (page: AppPage) => void }`
       - Reads from `useScenariosStore` (scenarios, selectedIds, isComparing, hasUnsavedChanges)
       - Reads from `useWizardStore` for current state access
       - Top bar: "+ New Calculation" button (resets wizard store to initial state and navigates to wizard page) and "Save Current" button
       - "Save Current" button: calls `scenariosStore.saveScenario()`, shows brief success toast/message
       - If scenarios.length >= MAX_SCENARIOS - 2, show amber warning: "You have {N}/20 saved scenarios"
       - If scenarios.length >= MAX_SCENARIOS, disable Save button with tooltip "Maximum 20 scenarios reached"
       - Scenario list: map scenarios to ScenarioCard components
       - Pass handlers to each card: onToggleSelect, onLoad, onDuplicate, onDelete, onRename
       - onLoad handler: check `hasUnsavedChanges()`. If true, show inline confirmation: "You have unsaved changes. Save current scenario before loading?" with Save & Load / Load Without Saving / Cancel. If false or confirmed, call `loadScenario(id)`, use `useWizardStore.setState(loadedState, true)` to replace wizard state, then `updateLastSavedHash()`, then navigate to wizard page.
       - "Clear All" button at bottom (only if scenarios.length > 0): red outline, shows stronger confirmation dialog: "This will permanently delete all {N} scenarios. Are you sure?"
       - Compare button: shown when `selectedIds.length === 2`, calls `startCompare()`
       - When `isComparing`, render ComparisonView (imported) below the list or replacing it
       - If no scenarios, show EmptyState component
       - Sorting: most recently updated first
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - App has page-level navigation between wizard and scenarios
    - AppLayout shows "Calculator" and "My Scenarios" in header and mobile bottom nav
    - ScenariosPage lists saved scenarios as compact cards with full CRUD operations
    - ScenarioCard shows name (editable), date, heir summary, estate value, adjustment badge, and action buttons
    - Delete has confirmation, load checks unsaved changes, duplicate adds "(Copy)" suffix
    - Empty state shown when no scenarios exist
    - Max 20 scenario warning shown at 18+ scenarios
  </done>
</task>

<task type="auto">
  <name>Task 2: ComparisonView with side-by-side layout and diff highlighting</name>
  <files>src/components/scenarios/ComparisonView.tsx, src/components/__tests__/scenarios.test.tsx</files>
  <action>
    1. Create `src/components/scenarios/ComparisonView.tsx`:
       - Props: `{ scenarioA: Scenario, scenarioB: Scenario, onClose: () => void }`
       - Header with "Comparing" title and Close/Back button
       - Two-column layout on desktop (`grid grid-cols-2 gap-4`), stacked on mobile (`grid grid-cols-1`)
       - Each column header: scenario name and date
       - Comparison sections:
         a. **Estate Summary**: total estate value per scenario, side by side
         b. **Adjustment**: show adjustment type (None/Awl/Radd) per scenario
         c. **Heir Shares Table**: rows for each unique heir type across both scenarios
            - For each heir type: show count, fraction (e.g. "1/6"), percentage, and BDT amount (if totalEstateValue > 0)
            - Use `scenarioA.state.results.shares` and `scenarioB.state.results.shares`
            - Build a unified heir list from both scenarios (union of heirTypes)
            - For heirs present in one but not other: show "--" in the missing column
       - **Diff highlighting**: for each row, compare the values between A and B. If `totalShare` fractions differ (or heir present in one but not other), apply `bg-amber-50` (pale amber) background to the entire row. No highlight when values match.
       - Fraction display: use `.toFraction()` for string representation, `.valueOf() * 100` for percentage
       - BDT display: `Intl.NumberFormat('en-IN', { style: 'currency', currency: 'BDT', currencyDisplay: 'narrowSymbol' })` -- use `Math.round(share.totalShare.valueOf() * totalEstateValue)`
       - Responsive: on mobile, each scenario becomes a stacked card with a clear "Scenario A" / "Scenario B" label
       - Clean data table feel: consistent column widths, subtle borders, no clutter

    2. Create `src/components/__tests__/scenarios.test.tsx`:
       - `beforeEach`: clear localStorage, reset both stores
       - Test ScenariosPage renders empty state when no scenarios
       - Test ScenariosPage shows scenario cards when scenarios exist (mock scenariosStore with 2 scenarios)
       - Test ScenarioCard renders name, date, heir summary, and estate value
       - Test ScenarioCard inline rename: click name, type new name, blur saves
       - Test ScenarioCard delete confirmation flow
       - Test ComparisonView renders two-column layout with heir shares from both scenarios
       - Test ComparisonView applies amber highlight on rows where shares differ
       - Test ComparisonView shows "--" for heirs present in only one scenario
       - Test "Save Current" button calls saveScenario
       - Test load scenario with unsaved changes shows confirmation
       - Use `@testing-library/react` render, screen, fireEvent/userEvent
       - Mock scenario data with real Fraction objects in results (import Fraction from 'fraction.js')
       - Create factory helper `createMockScenario(overrides)` for test data
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && npx vitest run src/components/__tests__/scenarios.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - ComparisonView shows two scenarios side by side on desktop, stacked on mobile
    - Each heir type row shows count, fraction, percentage, and BDT amount from both scenarios
    - Rows with differing values have pale amber background; matching rows have no highlight
    - Heirs present in only one scenario show "--" in the other column
    - All component tests pass: empty state, card rendering, rename, delete, compare, diff highlighting
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of persistence and scenarios</name>
  <action>
    What was built: Full persistence and scenarios system -- auto-save to localStorage, dedicated "My Scenarios" page with save/load/duplicate/delete/rename, scenario cards with heir summary, and side-by-side comparison view with diff highlighting. Navigation added to header and mobile bottom bar.

    How to verify:
    1. Open the app at http://localhost:5173
    2. Fill in some heir data (e.g., select "Father" relationship, add 2 sons, 1 daughter)
    3. Navigate to step 5 (Results) to generate calculation
    4. Refresh the page -- verify all inputs and results are preserved (auto-save working)
    5. Click "My Scenarios" in the top nav (or bottom nav on mobile)
    6. Click "Save Current" -- verify a scenario card appears with auto-generated name like "2 Sons, 1 Daughter -- Mar 13"
    7. Click the scenario name to rename it -- type a new name, press Enter -- verify it updates
    8. Click "Duplicate" -- verify a copy appears with "(Copy)" suffix
    9. Go back to Calculator, change inputs (e.g., add a wife), calculate again
    10. Go to My Scenarios, click "Save Current" again
    11. Select 2 scenarios via checkboxes, click "Compare" -- verify side-by-side view with heir shares
    12. Verify differing values have pale amber background
    13. Close comparison, click "Load" on a scenario -- if you have unsaved changes, verify the warning appears
    14. Click "Delete" on a scenario -- verify confirmation appears, confirm, verify it's removed
    15. Test on mobile viewport (375px): verify bottom nav works, comparison stacks vertically
    16. Verify "+ New Calculation" resets the wizard to step 1
  </action>
  <verify>Human visual and functional verification</verify>
  <done>User confirms all 16 verification steps pass correctly</done>
</task>

</tasks>

<verification>
```bash
cd /home/siam/Personal/jomi-bhag && npx vitest run --reporter=verbose
```
All tests pass: store tests (fractionStorage, wizardStore, scenariosStore) and component tests (scenarios page, comparison view).

```bash
cd /home/siam/Personal/jomi-bhag && npx tsc --noEmit
```
TypeScript compilation succeeds with no errors.
</verification>

<success_criteria>
- "My Scenarios" page accessible from top nav (desktop) and bottom nav (mobile)
- Scenario cards show name (editable), date, heir count summary, and estate value
- Save, load, duplicate, delete, rename, and clear-all operations work correctly
- Loading a scenario warns about unsaved changes when applicable
- Compare mode shows exactly 2 scenarios side by side with diff highlighting (pale amber on differences)
- Comparison is two-column on desktop, stacked on mobile
- "+ New Calculation" resets wizard to step 1 with empty state
- Empty state shown when no scenarios are saved
- All tests pass, TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/08-persistence-and-scenarios/08-02-SUMMARY.md`
</output>

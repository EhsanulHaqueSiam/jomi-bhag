---
phase: 03-core-results-display
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/results/AdjustmentBanner.tsx
  - src/components/results/SpecialCaseCallout.tsx
  - src/components/results/BlockedHeirsSection.tsx
  - src/components/results/StepAccordion.tsx
  - src/components/results/IslamicBasisSection.tsx
  - src/components/results/ResultsPage.tsx
  - src/components/__tests__/results.test.tsx
autonomous: false
requirements:
  - RSLT-03
  - RSLT-06

must_haves:
  truths:
    - "User sees a step-by-step calculation explanation in detailed mode"
    - "User can expand multiple accordion steps simultaneously to see full detail"
    - "Detailed mode shows grouped Islamic Basis section with all unique references"
    - "Simple mode hides the step accordion and Islamic Basis section"
    - "Awl/Radd adjustment banner explains what happened at the top of results"
    - "Special cases (Umariyyatayn, Mushtarakah, Kalalah) show highlighted callout boxes"
    - "Blocked heirs section explains who was blocked, by whom, and why"
  artifacts:
    - path: "src/components/results/StepAccordion.tsx"
      provides: "Numbered accordion for calculation steps with animated expand/collapse"
      min_lines: 40
    - path: "src/components/results/IslamicBasisSection.tsx"
      provides: "Grouped display of all unique Quran/Hadith references"
      min_lines: 25
    - path: "src/components/results/AdjustmentBanner.tsx"
      provides: "Info banner for Awl/Radd adjustments"
      min_lines: 20
    - path: "src/components/results/SpecialCaseCallout.tsx"
      provides: "Highlighted callout for special cases"
      min_lines: 25
    - path: "src/components/results/BlockedHeirsSection.tsx"
      provides: "Educational section showing blocked heirs with reasons"
      min_lines: 20
    - path: "src/components/__tests__/results.test.tsx"
      provides: "Integration tests for RSLT-01, RSLT-02, RSLT-03, RSLT-06"
      min_lines: 80
  key_links:
    - from: "src/components/results/ResultsPage.tsx"
      to: "src/components/results/StepAccordion.tsx"
      via: "Conditional render when viewMode === 'detailed'"
      pattern: "viewMode.*detailed.*StepAccordion"
    - from: "src/components/results/ResultsPage.tsx"
      to: "src/components/results/IslamicBasisSection.tsx"
      via: "Conditional render when viewMode === 'detailed'"
      pattern: "viewMode.*detailed.*IslamicBasisSection"
    - from: "src/components/results/StepAccordion.tsx"
      to: "motion/react"
      via: "AnimatePresence + height auto animation"
      pattern: "AnimatePresence"
    - from: "src/components/results/ResultsPage.tsx"
      to: "src/components/results/AdjustmentBanner.tsx"
      via: "Renders when output.adjustment !== 'none'"
      pattern: "adjustment.*none"
---

<objective>
Complete the results display with educational supplementary sections (blocked heirs, Awl/Radd banners, special case callouts), step-by-step calculation accordion for detailed mode, grouped Islamic references, and integration tests.

Purpose: This plan finishes Phase 3 by adding all detailed-mode content and the supplementary educational sections that make the results trustworthy and informative. After this plan, both simple and detailed modes are fully functional.

Output: Complete results page with all sections, integration tests proving all RSLT requirements, and visual verification.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-results-display/03-CONTEXT.md
@.planning/phases/03-core-results-display/03-RESEARCH.md
@.planning/phases/03-core-results-display/03-01-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01 that this plan builds on. -->

From src/core/faraid/types.ts:
```typescript
export interface FaraidOutput {
  shares: ShareResult[]
  adjustment: AdjustmentType  // 'none' | 'awl' | 'radd'
  totalBeforeAdjustment: Fraction
  blockedHeirs: { heirType: HeirType; blockedBy: HeirType; rule: string }[]
  specialCases: string[]  // e.g., ['kalalah', 'umariyyatayn', 'mushtarakah']
  mfloApplied: boolean
  steps: CalculationStep[]
  references: IslamicReference[]
}

export interface CalculationStep {
  step: number
  description: string
  detail: string
}

export interface IslamicReference {
  type: 'quran' | 'hadith'
  reference: string
  arabicText: string
  englishText: string
  appliesTo: HeirType[]
}
```

From src/core/faraid/references.ts:
```typescript
export function getAllReferences(output: FaraidOutput): IslamicReference[]
export function getAdjustmentReference(adjustmentType: 'awl' | 'radd'): IslamicReference
```

From src/core/utils/display.ts (created in Plan 01):
```typescript
export function fractionToString(f: Fraction): string
export function fractionToPercent(f: Fraction, decimals?: number): string
export function fractionToBDT(f: Fraction, totalEstate: number): string
export const HEIR_TYPE_LABELS: Record<HeirType, string>
export const SHARE_TYPE_LABELS: Record<string, string>
```

From src/stores/wizardStore.ts (extended in Plan 01):
```typescript
// New state fields:
results: FaraidOutput | null
totalEstateValue: number
viewMode: 'simple' | 'detailed'

// New actions:
calculateShares: () => void
setTotalEstateValue: (value: number) => void
setViewMode: (mode: 'simple' | 'detailed') => void
```

From src/components/results/ResultsPage.tsx (created in Plan 01):
```typescript
// Has placeholder comments for:
// {/* Plan 02: AdjustmentBanner */}
// {/* Plan 02: SpecialCaseCallout */}
// {/* Plan 02: BlockedHeirsSection */}
// {/* Plan 02: StepAccordion, IslamicBasisSection */}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supplementary sections -- banners, callouts, blocked heirs</name>
  <files>src/components/results/AdjustmentBanner.tsx, src/components/results/SpecialCaseCallout.tsx, src/components/results/BlockedHeirsSection.tsx, src/components/results/ResultsPage.tsx</files>
  <action>
**1. Create src/components/results/AdjustmentBanner.tsx:**
- Props: `adjustment: AdjustmentType, totalBeforeAdjustment: Fraction`
- If `adjustment === 'none'`, render nothing
- Colored banner: amber/warm background for Awl (shares exceeded 1, proportionally reduced), blue/cool background for Radd (surplus redistributed to eligible heirs)
- Content:
  - Awl: "Awl (Proportional Reduction) Applied" heading. Text: "The total prescribed shares exceeded the estate ({fractionToString(totalBeforeAdjustment)}). All shares have been proportionally reduced so each heir receives a fair portion." Include a small info icon.
  - Radd: "Radd (Surplus Return) Applied" heading. Text: "The total prescribed shares were less than the full estate ({fractionToString(totalBeforeAdjustment)}). The surplus has been redistributed to eligible blood-relative heirs. Spouses are excluded from Radd per Hanafi jurisprudence."
- Styling: rounded-xl, border-l-4 with accent color, p-4, icon + text layout. Consistent with Phase 2's clean aesthetic.

**2. Create src/components/results/SpecialCaseCallout.tsx:**
- Props: `specialCases: string[]`
- If empty array, render nothing
- For each special case, render a callout box with gold border and Islamic accent styling
- Use the following content map:
  - `kalalah`: Title "Kalalah", Description: "The deceased has no children, no father, and no grandfather. Siblings inherit according to Quran 4:12 (uterine) and 4:176 (full/consanguine)."
  - `umariyyatayn`: Title "Umariyyatayn (Two Omari Cases)", Description: "Per Omar's (RA) ruling: the mother receives 1/3 of the remainder after the spouse's share, not 1/3 of the entire estate."
  - `mushtarakah`: Title "Mushtarakah (Shared Case)", Description: "Hanafi ruling: full siblings receive nothing as Asaba when there is no remainder. Note: Shafi'i and Maliki schools would have full siblings share in the uterine siblings' 1/3 portion."
- Styling: gold-100 background, gold-600 left border (4px), gold-700 title text, rounded-xl, p-4. A small decorative Islamic pattern or star icon in the top corner.

**3. Create src/components/results/BlockedHeirsSection.tsx:**
- Props: `blockedHeirs: FaraidOutput['blockedHeirs']`
- If empty array, render nothing
- Section heading: "Blocked Heirs" with a subtle gray divider
- For each blocked heir: a compact row showing:
  - Heir type name (from HEIR_TYPE_LABELS)
  - "blocked by" text
  - Blocker name (from HEIR_TYPE_LABELS)
  - Rule explanation in parentheses (the `rule` field from the blocked heir object)
- Educational tone: a small info paragraph at top: "The following heirs are excluded from inheritance in this scenario due to Islamic blocking rules (Hajb Hirman). A closer relative takes their place."
- Styling: gray-50 background, rounded-xl, p-4, text-sm for individual entries, text-gray-600 for explanatory text

**4. Update src/components/results/ResultsPage.tsx:**
- Import AdjustmentBanner, SpecialCaseCallout, BlockedHeirsSection
- Replace Plan 02 placeholder comments with actual component renders:
  - AdjustmentBanner: render above the heir cards, pass `results.adjustment` and `results.totalBeforeAdjustment`
  - SpecialCaseCallout: render between AdjustmentBanner and heir cards, pass `results.specialCases`
  - BlockedHeirsSection: render below the heir card grid, pass `results.blockedHeirs`
- Keep the detailed mode placeholders for StepAccordion and IslamicBasisSection (Task 2 will fill those)
- Layout order from top to bottom: Header (title + ModeToggle) -> Edit Heirs button -> EstateValueInput -> AdjustmentBanner -> SpecialCaseCallout -> HeirCard grid -> BlockedHeirsSection -> [detailed: StepAccordion, IslamicBasisSection]
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && npx tsc --noEmit && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>AdjustmentBanner renders for Awl/Radd cases with educational explanations. SpecialCaseCallout renders gold-bordered boxes for special cases. BlockedHeirsSection shows blocked heirs with reasons. All three integrated into ResultsPage. No regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Step accordion, Islamic Basis section, and integration tests</name>
  <files>src/components/results/StepAccordion.tsx, src/components/results/IslamicBasisSection.tsx, src/components/results/ResultsPage.tsx, src/components/__tests__/results.test.tsx</files>
  <action>
**1. Create src/components/results/StepAccordion.tsx:**
- Props: `steps: CalculationStep[]`
- Numbered accordion: vertical list where each item shows:
  - Always visible: step number in a circle (emerald) + description text
  - Expandable: detail text (the full calculation explanation)
- State: `openSteps: Set<number>` -- tracks which steps are expanded. Multiple can be open simultaneously (per user decision).
- Toggle: clicking a step header toggles it in/out of the Set
- Animation: Use `motion/react` AnimatePresence + `motion.div` with `initial={{ height: 0, opacity: 0 }}`, `animate={{ height: 'auto', opacity: 1 }}`, `exit={{ height: 0, opacity: 0 }}`, `transition={{ duration: 0.3, ease: 'easeInOut' }}`. CRITICAL: set `style={{ overflow: 'hidden' }}` on the animated wrapper.
- Step detail content: render as plain text with the educational tone. If detail contains fraction notation (e.g., "1/2 + 1/6 = 2/3"), render as-is -- the engine already formats these.
- Chevron icon rotates 180 degrees when expanded (CSS transform + transition)
- Section heading: "Calculation Steps" with a divider
- Styling: each step item has a subtle border-b, p-3/p-4 for comfortable spacing

**2. Create src/components/results/IslamicBasisSection.tsx:**
- Props: `references: IslamicReference[]`
- If empty, render nothing
- Section heading: "Islamic Basis" with a divider and a small description: "All Quranic verses and Hadith references applied in this calculation"
- For each reference, render a card with:
  - Type badge: "Quran" (emerald) or "Hadith" (gold)
  - Reference label (e.g., "Quran 4:12", "Bukhari 6732")
  - Arabic text in `<p dir="rtl" lang="ar" className="font-arabic">` with Noto Naskh Arabic font, gold-50 background, rounded padding. Skip if arabicText is empty.
  - English translation below in regular text-sm font
  - "Applies to:" showing heir type names (from HEIR_TYPE_LABELS) if appliesTo is not empty
- Styling: subtle grid or vertical stack, each reference card has light background and rounded corners

**3. Update src/components/results/ResultsPage.tsx:**
- Import StepAccordion, IslamicBasisSection
- Import `getAllReferences` from `@/core/faraid/references`
- Replace the detailed mode placeholder with actual conditional rendering:
  ```
  {viewMode === 'detailed' && (
    <>
      <StepAccordion steps={results.steps} />
      <IslamicBasisSection references={getAllReferences(results)} />
    </>
  )}
  ```
- Ensure the detailed sections appear after BlockedHeirsSection in the layout order

**4. Create src/components/__tests__/results.test.tsx:**
- Import from `@testing-library/react`, `vitest`, and `@testing-library/jest-dom`
- Create a mock `FaraidOutput` factory function that produces realistic test data:
  - Simple scenario: husband + 2 daughters, standard shares
  - Awl scenario: shares exceeding 1
  - Radd scenario: surplus redistribution
  - Blocked heirs scenario: with at least 1 blocked heir
  - Special case scenario: with kalalah
- Set up wizardStore state before each test using `useWizardStore.setState()`
- Tests must cover:
  - RSLT-01: "displays fraction percentage and monetary" -- render ResultsPage with results in store, verify heir cards show fraction text, percentage text, and BDT amount when totalEstateValue > 0
  - RSLT-02: "shows Quranic reference" -- render ResultsPage, find the reference label (e.g., "Quran 4:12"), click to expand, verify Arabic text container has `dir="rtl"` and English translation is visible
  - RSLT-03: "step accordion" -- set viewMode to 'detailed' in store, render ResultsPage, verify step descriptions are visible, click a step header, verify detail text expands
  - RSLT-06: "mode toggle" -- render ResultsPage, verify step accordion is NOT visible in simple mode, click "Detailed" in the segmented control, verify step accordion IS visible, click "Simple", verify it hides again
  - Additional: blocked heirs section renders when blockedHeirs is non-empty, adjustment banner renders for Awl/Radd
- Use `screen.getByText`, `screen.queryByText`, `fireEvent.click` patterns consistent with existing wizard.test.tsx
- IMPORTANT: Zustand store must be reset between tests. Use `beforeEach(() => useWizardStore.setState({ ...initialState, results: mockOutput }))` pattern.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && npx vitest run src/components/__tests__/results.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>StepAccordion renders numbered calculation steps with animated expand/collapse. IslamicBasisSection shows all unique references with Arabic text. Both appear only in detailed mode. Integration tests pass for RSLT-01 (fraction/percent/BDT), RSLT-02 (Quran refs), RSLT-03 (step accordion), RSLT-06 (mode toggle).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete results display</name>
  <files></files>
  <action>
Human verifies the complete Phase 3 results display end-to-end. All components built in Tasks 1-2 and Plan 01 are visually inspected for correctness, responsiveness, and proper Islamic text rendering.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>User has approved the visual appearance and functionality of the complete results display.</done>
  <what-built>Complete Phase 3 results display: heir cards with fraction/percentage/BDT, expandable Quran references with Arabic text, Awl/Radd banners, special case callouts, blocked heirs section, step-by-step calculation accordion in detailed mode, and Simple/Detailed mode toggle.</what-built>
  <how-to-verify>
    1. Run `npx vite dev` and open http://localhost:5173
    2. Complete wizard: select a relationship (e.g., "Brother"), add 1 wife, 2 sons, 1 daughter on step 2, skip siblings on step 3
    3. Click "Calculate Shares" -- verify results page appears with heir cards
    4. Verify each card shows: heir type name, share fraction (e.g., "1/8"), percentage (e.g., "12.5%"), and share type badge
    5. Enter "5000000" in the estate value input -- verify all cards show BDT amounts with lakh grouping (e.g., "৳6,25,000")
    6. Expand a Quran reference on any card -- verify Arabic text renders right-to-left with proper font, and English translation is visible below
    7. Toggle to "Detailed" mode -- verify step-by-step calculation accordion appears and "Islamic Basis" section appears at bottom
    8. Expand multiple accordion steps -- verify they stay open simultaneously and animation is smooth
    9. Toggle back to "Simple" mode -- verify accordion and Islamic Basis section disappear
    10. Click "Edit Heirs" -- verify wizard returns to step 1 with all previous inputs preserved
    11. On mobile (375px viewport): verify cards stack single-column, controls are tappable, no content cut off
    12. Test an Awl scenario (e.g., husband + 2 daughters + mother): verify adjustment banner appears with explanation
    13. Test a blocked heir scenario: verify blocked heirs section appears below active heir cards
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npx vitest run` -- all tests pass including new results integration tests
3. `npx vitest run src/components/__tests__/results.test.tsx` -- all 6+ results-specific tests pass
4. Visual: results page renders correctly with all sections in both simple and detailed modes
5. Visual: Arabic text renders RTL with correct Noto Naskh Arabic font
6. Visual: Animations are smooth (accordion, Quran reference expand/collapse)
7. Visual: Mobile responsive -- no breakage at 375px
</verification>

<success_criteria>
- RSLT-03: Step-by-step accordion shows numbered calculation steps with expandable detail, visible only in detailed mode
- RSLT-06: Simple mode shows heir cards + blocked heirs + banners. Detailed mode adds step accordion + Islamic Basis section. Toggle works correctly.
- Awl/Radd banner appears when adjustment is applied with educational explanation
- Special case callouts appear with gold borders for Umariyyatayn/Mushtarakah/Kalalah
- Blocked heirs section lists excluded heirs with blocking rules
- All integration tests pass
- Full test suite remains green (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-results-display/03-02-SUMMARY.md`
</output>

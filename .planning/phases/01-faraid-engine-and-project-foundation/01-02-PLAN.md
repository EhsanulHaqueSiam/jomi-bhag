---
phase: 01-faraid-engine-and-project-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/core/faraid/blocking.ts
  - src/core/faraid/shares.ts
  - src/core/faraid/special-cases.ts
  - src/core/faraid/__tests__/blocking.test.ts
  - src/core/faraid/__tests__/shares.test.ts
  - src/core/faraid/__tests__/special.test.ts
autonomous: true
requirements:
  - FARD-01
  - FARD-05
  - FARD-06
  - FARD-07
  - FARD-08

must_haves:
  truths:
    - "Every Hajb Hirman rule correctly blocks the blocked heir when the blocker is present"
    - "Every Hajb Nuqsan rule correctly reduces the heir's share when the trigger is present"
    - "Fixed share assignment returns the correct Quranic share for each heir based on who else is present"
    - "Full, consanguine, and uterine siblings receive different shares according to their type"
    - "Umariyyatayn correctly gives mother 1/3 of remainder, not 1/3 of estate"
    - "Mushtarakah follows strict Hanafi with annotation about alternative opinion"
    - "Grandfather blocks all siblings in Hanafi jurisprudence (rule 8)"
  artifacts:
    - path: "src/core/faraid/blocking.ts"
      provides: "All 16 Hajb Hirman + 5 Hajb Nuqsan rules as data-driven lookup with applyHajb function"
      exports: ["applyHajb", "BLOCKING_RULES", "REDUCTION_RULES"]
      min_lines: 100
    - path: "src/core/faraid/shares.ts"
      provides: "Fixed share (fard) assignment based on heir context using the rule table"
      exports: ["assignFixedShares"]
      min_lines: 60
    - path: "src/core/faraid/special-cases.ts"
      provides: "Umariyyatayn, Mushtarakah, and Kalalah detection and handling"
      exports: ["detectSpecialCases", "applyUmariyyatayn", "applyMushtarakah", "isKalalah"]
      min_lines: 50
    - path: "src/core/faraid/__tests__/blocking.test.ts"
      provides: "Test for each of the 16 total blocking rules and 5 partial reduction rules"
      min_lines: 200
    - path: "src/core/faraid/__tests__/shares.test.ts"
      provides: "Test for fixed share assignment across all heir types"
      min_lines: 100
    - path: "src/core/faraid/__tests__/special.test.ts"
      provides: "Tests for Umariyyatayn, Mushtarakah, and Kalalah"
      min_lines: 60
  key_links:
    - from: "src/core/faraid/blocking.ts"
      to: "src/core/faraid/types.ts"
      via: "imports BlockingRule, HeirType, HeirContext"
      pattern: "import.*from.*types"
    - from: "src/core/faraid/shares.ts"
      to: "src/data/faraid-rules.ts"
      via: "reads declarative rule table to assign shares"
      pattern: "import.*from.*faraid-rules"
    - from: "src/core/faraid/shares.ts"
      to: "src/core/utils/fraction.ts"
      via: "uses named fraction constants"
      pattern: "import.*from.*fraction"
    - from: "src/core/faraid/special-cases.ts"
      to: "src/core/faraid/shares.ts"
      via: "modifies share assignments for special cases"
      pattern: "import.*from.*shares"
---

<objective>
Implement all 16 Hajb Hirman (total blocking) rules, all 5 Hajb Nuqsan (partial reduction) rules, fixed share (fard) assignment for all 12 Dhawul Furud heir types, and the three special cases (Umariyyatayn, Mushtarakah, Kalalah). This is the core calculation logic that determines WHO gets WHAT before any adjustments.

Purpose: Blocking must run before share assignment (anti-pattern from RESEARCH.md). Shares depend on types and rules from Plan 01. Adjustments (Plan 03) depend on correct shares from this plan. This is the critical middle layer.

Output: Working blocking module, share assignment module, and special case handlers, all with comprehensive tests.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-faraid-engine-and-project-foundation/01-CONTEXT.md
@.planning/phases/01-faraid-engine-and-project-foundation/01-RESEARCH.md
@.planning/phases/01-faraid-engine-and-project-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes. -->
<!-- Executor should import these directly -- no codebase exploration needed. -->

From src/core/faraid/types.ts:
- HeirType: union of all 17 heir type strings
- HeirInput: { type: HeirType; count: number }
- HeirContext: boolean flags and counts for all heir types (derived from FaraidInput)
- FaraidInput: { deceasedGender: 'male' | 'female'; heirs: HeirInput[]; mfloEnabled?: boolean }
- ShareResult: { heirType, count, sharePerHeir: Fraction, totalShare: Fraction, shareType, quranRef?, hadithRef?, explanation, notes? }
- BlockingRule: { blocker, blocked, type: 'total' | 'partial', condition?, note? }
- ShareCondition: { share: Fraction, when: string, predicate: (ctx: HeirContext) => boolean }
- FaraidRule: { heirType, shareType, conditions: ShareCondition[], quranRef, hadithRef? }

From src/core/utils/fraction.ts:
- HALF, QUARTER, EIGHTH, TWO_THIRDS, ONE_THIRD, ONE_SIXTH, ZERO, ONE
- sumFractions(fractions: Fraction[]): Fraction

From src/data/faraid-rules.ts:
- Complete declarative rule table indexed by HeirType
- Each rule has ShareCondition[] with predicate functions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hajb blocking rules -- all 16 total blocking + 5 partial reduction</name>
  <files>
    src/core/faraid/blocking.ts,
    src/core/faraid/__tests__/blocking.test.ts
  </files>
  <behavior>
    **Hajb Hirman (16 total blocking rules -- each must have a dedicated test):**
    - Test 1: Son present -> son's son blocked
    - Test 2: Son's son present -> son's son's son blocked (if modeled)
    - Test 3: Son present -> daughter(s) of son blocked
    - Test 4: Son's son present -> daughter(s) of son's son blocked (if modeled)
    - Test 5: Two+ daughters present, no son's son -> daughter(s) of son blocked
    - Test 6: Father present -> paternal grandfather blocked
    - Test 7: Father present -> all siblings (full, consanguine, uterine) blocked
    - Test 8: Paternal grandfather present -> all siblings blocked (HANAFI SPECIFIC)
    - Test 9: Full brother present -> consanguine brother blocked
    - Test 10: Full brother present -> consanguine sister blocked
    - Test 11: Full sister as Asaba ma'a ghayrihi -> consanguine siblings blocked
    - Test 12: Son/daughter/father present -> uterine siblings blocked
    - Test 13: Paternal grandfather present -> uterine siblings blocked
    - Test 14: Mother present -> maternal grandmother blocked
    - Test 15: Mother present -> paternal grandmother blocked
    - Test 16: Father present -> paternal grandmother blocked (HANAFI SPECIFIC)

    **Hajb Nuqsan (5 partial reduction rules):**
    - Test 17: Husband reduced from 1/2 to 1/4 when children/grandchildren exist
    - Test 18: Wife reduced from 1/4 to 1/8 when children/grandchildren exist
    - Test 19: Mother reduced from 1/3 to 1/6 when children, grandchildren, or 2+ siblings exist
    - Test 20: Single daughter of son reduced from 1/2 to 1/6 when single daughter present
    - Test 21: Single consanguine sister reduced from 1/2 to 1/6 when single full sister present

    **Edge cases:**
    - Test 22: Heir not present in input is NOT listed as blocked (no phantom blocking)
    - Test 23: Multiple blocking rules applied simultaneously
    - Test 24: Blocking does NOT affect the blocker's own share
  </behavior>
  <action>
    **src/core/faraid/blocking.ts:**

    1. Define `TOTAL_BLOCKING_RULES` as a const array of BlockingRule objects, one per rule from the RESEARCH.md "Complete Hajb Hirman" table. Each rule has:
       - `blocker`: HeirType or HeirType[] that triggers blocking
       - `blocked`: HeirType or HeirType[] that gets blocked
       - `type`: 'total'
       - `condition`: optional string for conditional blocking (e.g., rule 5 requires 2+ daughters AND no son's son)
       - `note`: optional string for educational annotation (e.g., rule 8: "Hanafi specific -- other schools differ")

    2. Define `PARTIAL_REDUCTION_RULES` as a const array for the 5 Hajb Nuqsan rules. Each has:
       - `heir`: HeirType whose share is reduced
       - `originalShare`: Fraction (the unreduced share)
       - `reducedShare`: Fraction (the reduced share)
       - `trigger`: string describing what triggers reduction
       - `predicate`: `(ctx: HeirContext) => boolean` that checks the trigger condition

    3. Implement `applyHajb(heirs: HeirInput[], ctx: HeirContext): { activeHeirs: HeirInput[]; blocked: Array<{ heirType: HeirType; blockedBy: HeirType; rule: string }>; reductions: Array<{ heirType: HeirType; from: Fraction; to: Fraction; reason: string }> }`:
       - Iterate TOTAL_BLOCKING_RULES: for each rule, if blocker is present in heirs and blocked is present, remove blocked from active heirs and record in blocked array
       - Rule 5 (2+ daughters block daughter of son) has a special condition: if son's son is also present, daughter of son is NOT blocked (she becomes Asaba bi-ghayrihi through son's son)
       - Return the filtered active heirs, list of blocked heirs with reasons, and list of reductions to apply during share assignment

    4. Export TOTAL_BLOCKING_RULES and PARTIAL_REDUCTION_RULES for transparency/testing.

    CRITICAL: Rule 8 (grandfather blocks siblings) is Hanafi-specific. Include note: "Hanafi specific -- Shafi'i and Maliki schools differ." This is a locked user decision.

    Write tests FIRST. Each of the 16 total blocking rules gets its own test case. Each of the 5 partial reduction rules gets its own test case.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && bun run vitest run src/core/faraid/__tests__/blocking.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - All 16 Hajb Hirman rules tested individually and passing (FARD-05)
    - All 5 Hajb Nuqsan rules tested individually and passing (FARD-06)
    - Hanafi-specific rules (8, 16) annotated with notes (FARD-08)
    - Full/consanguine/uterine siblings blocked differently per their type (FARD-07)
    - Blocking runs as data-driven lookup, not hardcoded if/else chains
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixed share assignment and special cases (Umariyyatayn, Mushtarakah, Kalalah)</name>
  <files>
    src/core/faraid/shares.ts,
    src/core/faraid/special-cases.ts,
    src/core/faraid/__tests__/shares.test.ts,
    src/core/faraid/__tests__/special.test.ts
  </files>
  <behavior>
    **Fixed share assignment:**
    - Test: Single daughter, no sons -> gets 1/2
    - Test: Two daughters, no sons -> get 2/3 (shared)
    - Test: Husband, no children -> gets 1/2
    - Test: Husband, with children -> gets 1/4
    - Test: Wife, no children -> gets 1/4
    - Test: Wife, with children -> gets 1/8
    - Test: Two wives, with children -> each gets 1/16 (1/8 divided equally)
    - Test: Mother, no children, 0-1 siblings -> gets 1/3
    - Test: Mother, with children -> gets 1/6
    - Test: Mother, no children, 2+ siblings -> gets 1/6
    - Test: Father, with children -> gets 1/6
    - Test: Father, with daughters only (no sons) -> gets 1/6 + residuary
    - Test: Father, no children -> Asaba only (handled in Plan 03, but marked here)
    - Test: Single full sister, no children, no father -> gets 1/2
    - Test: Two+ full sisters, same conditions -> get 2/3 (shared)
    - Test: Single consanguine sister, no full siblings -> gets 1/2
    - Test: Consanguine sister with one full sister -> gets 1/6 (completing 2/3)
    - Test: Single uterine brother, Kalalah -> gets 1/6
    - Test: Two+ uterine siblings, Kalalah -> share 1/3
    - Test: Grandmother (paternal or maternal) -> gets 1/6

    **Special cases:**
    - Test: Umariyyatayn Case 1: husband(1/2) + mother + father -> mother gets 1/6 (1/3 of remainder 1/2)
    - Test: Umariyyatayn Case 2: wife(1/4) + mother + father -> mother gets 1/4 (1/3 of remainder 3/4)
    - Test: Mushtarakah: husband + mother + uterine siblings + full siblings -> full siblings get nothing (Hanafi), output includes note about Shafi'i/Maliki alternative
    - Test: Kalalah detection: no children, no father -> isKalalah returns true
    - Test: Non-Kalalah: children present -> isKalalah returns false
  </behavior>
  <action>
    **src/core/faraid/shares.ts:**

    1. Implement `buildHeirContext(input: FaraidInput): HeirContext` -- convert FaraidInput into a flat context object with boolean flags (hasSons, hasDaughters, hasFather, etc.) and counts (sonCount, daughterCount, etc.) for predicate evaluation.

    2. Implement `assignFixedShares(activeHeirs: HeirInput[], ctx: HeirContext, reductions: ReductionInfo[]): ShareAssignment[]`:
       - For each active heir, look up their FaraidRule from the rule table (src/data/faraid-rules.ts)
       - Evaluate conditions in order, take the first matching condition's share
       - Apply any Hajb Nuqsan reductions from the reductions array
       - For polygamy: if wife count > 1, the wife's total share (1/4 or 1/8) is divided equally among all wives. Each wife's sharePerHeir = totalWifeShare / wifeCount
       - Return ShareAssignment[] with per-heir fractions, NOT yet adjusted for Awl/Radd
       - Mark heirs that are Asaba (residuary) separately -- they do NOT get a fixed share here, they get remainder in Plan 03

    3. Each ShareAssignment includes: heirType, count, sharePerHeir, totalShare, explanation (human-readable), quranRef/hadithRef from the rule table.

    **src/core/faraid/special-cases.ts:**

    1. `isKalalah(ctx: HeirContext): boolean` -- returns true if deceased has no children AND no father. This affects uterine sibling shares and full/consanguine sibling rules (4:176 vs 4:12).

    2. `detectSpecialCases(ctx: HeirContext): string[]` -- returns array of detected special case names: 'umariyyatayn', 'mushtarakah', 'kalalah'.

    3. `applyUmariyyatayn(shares: ShareAssignment[], ctx: HeirContext): ShareAssignment[]`:
       - Detect pattern: spouse + mother + father, NO children, NO siblings
       - Mother's share = 1/3 of REMAINDER after spouse's share (not 1/3 of estate)
       - Case 1 (husband): remainder = 1 - 1/2 = 1/2, mother = 1/3 * 1/2 = 1/6, father = remainder = 1/3
       - Case 2 (wife): remainder = 1 - 1/4 = 3/4, mother = 1/3 * 3/4 = 1/4, father = remainder = 1/2
       - Add explanation: "Umariyyatayn: per Omar's (RA) ruling, mother receives 1/3 of remainder after spouse's share"

    4. `applyMushtarakah(shares: ShareAssignment[], ctx: HeirContext): ShareAssignment[]`:
       - Detect pattern: husband + mother + 2+ uterine siblings + full siblings present
       - Hanafi: full siblings get NOTHING (uterine siblings take their 1/3)
       - Add note to output: "Shafi'i and Maliki alternative: full siblings share in the uterine siblings' 1/3 portion. This tool follows the Hanafi ruling."
       - Per locked user decision: show note with alternative opinion

    Write tests FIRST. Use the exact Umariyyatayn worked examples from RESEARCH.md to verify correctness.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && bun run vitest run src/core/faraid/__tests__/shares.test.ts src/core/faraid/__tests__/special.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Fixed shares correctly assigned for all 12 Dhawul Furud heir types using rule table predicates (FARD-01)
    - Polygamy handled: wife share divided equally among multiple wives
    - Full/consanguine/uterine siblings get distinct shares based on type (FARD-07)
    - Umariyyatayn gives mother 1/3 of remainder per Omar's ruling (FARD-08 Hanafi)
    - Mushtarakah follows strict Hanafi with annotated alternative opinion (FARD-08, locked decision)
    - Kalalah correctly detected and handled (FARD-08)
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `bun run vitest run src/core/faraid/__tests__/blocking.test.ts --reporter=verbose` -- all 16+5+3 tests pass (FARD-05, FARD-06)
2. `bun run vitest run src/core/faraid/__tests__/shares.test.ts --reporter=verbose` -- all fixed share tests pass (FARD-01, FARD-07)
3. `bun run vitest run src/core/faraid/__tests__/special.test.ts --reporter=verbose` -- Umariyyatayn, Mushtarakah, Kalalah tests pass (FARD-08)
4. Hanafi-specific rule 8 (grandfather blocks siblings) has test + annotation
5. Mushtarakah output includes Shafi'i/Maliki alternative opinion note
6. No floating-point arithmetic -- all shares use Fraction objects
</verification>

<success_criteria>
- All 16 Hajb Hirman total blocking rules implemented and tested individually (FARD-05)
- All 5 Hajb Nuqsan partial reduction rules implemented and tested (FARD-06)
- Fixed share assignment works for all 12 heir types with correct conditional logic (FARD-01)
- Sibling types (full/consanguine/uterine) handled distinctly throughout (FARD-07)
- All Hanafi-specific rules annotated and tested (FARD-08)
- Special cases (Umariyyatayn, Mushtarakah, Kalalah) correctly detected and handled
- Blocking runs BEFORE share assignment (architecture enforced)
</success_criteria>

<output>
After completion, create `.planning/phases/01-faraid-engine-and-project-foundation/01-02-SUMMARY.md`
</output>

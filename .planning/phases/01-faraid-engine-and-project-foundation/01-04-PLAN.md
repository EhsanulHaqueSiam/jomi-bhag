---
phase: 01-faraid-engine-and-project-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/faraid/shares.ts
  - src/core/faraid/special-cases.ts
  - src/core/faraid/__tests__/special.test.ts
autonomous: true
gap_closure: true
requirements:
  - FARD-01
  - FARD-02
  - FARD-03
  - FARD-04
  - FARD-05
  - FARD-06
  - FARD-07
  - FARD-08
  - DSGN-04

must_haves:
  truths:
    - "bun run build completes with exit code 0 (no TypeScript errors)"
    - "dist/index.html exists and reflects the current source"
    - "All 158 existing tests still pass after import cleanup"
  artifacts:
    - path: "src/core/faraid/shares.ts"
      provides: "Fixed share assignment module with clean imports"
      contains: "assignFixedShares"
    - path: "src/core/faraid/special-cases.ts"
      provides: "Special cases module with clean imports"
      contains: "applyUmariyyatayn"
    - path: "src/core/faraid/__tests__/special.test.ts"
      provides: "Special cases test file with clean imports"
      contains: "describe"
    - path: "dist/index.html"
      provides: "Built static site artifact"
  key_links:
    - from: "src/core/faraid/shares.ts"
      to: "src/core/faraid/types.ts"
      via: "import type (no value import of FaraidRule)"
      pattern: "import type.*FaraidRule"
    - from: "src/core/faraid/special-cases.ts"
      to: "src/core/utils/fraction.ts"
      via: "import only used constants"
      pattern: "import.*ONE.*ZERO.*ONE_THIRD"
---

<objective>
Fix 6 TypeScript unused-import errors across 3 source files that block `bun run build`.

Purpose: The VERIFICATION.md report found that success criterion 5 ("The project builds, passes linting, and deploys as a static site to Netlify") fails because `tsc -b` reports 6 unused-import errors. The engine logic is correct and all 158 tests pass, but the build cannot complete. Removing unused imports will produce a clean build.

Output: Clean `bun run build` producing `dist/index.html`, all tests still passing.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-faraid-engine-and-project-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused imports and verify clean build</name>
  <files>src/core/faraid/shares.ts, src/core/faraid/special-cases.ts, src/core/faraid/__tests__/special.test.ts</files>
  <action>
Fix 6 TypeScript unused-import errors identified in VERIFICATION.md:

1. In `src/core/faraid/shares.ts` line 19: Remove `FaraidRule` from the value import on lines 14-21. It is imported via `import type { ... }` but listed alongside value imports. Since `FaraidRule` is only referenced in type positions (not used as a runtime value), either remove it from the import entirely or ensure it stays in the `import type` block. Currently lines 14-21 are already `import type { ... }`, so the fix is to remove `FaraidRule` from the list since it is never referenced anywhere in the file (TS6196 says it was "imported but never used as a value" which in this context means it is simply never used at all in the file).

2. In `src/core/faraid/special-cases.ts` line 14: Remove the entire `import Fraction from 'fraction.js'` line. The file does not use `Fraction` directly (it uses the named constants from fraction.ts instead).

3. In `src/core/faraid/special-cases.ts` line 15: Remove `HALF`, `QUARTER`, and `ONE_SIXTH` from the import `{ ONE, ZERO, ONE_THIRD, HALF, QUARTER, ONE_SIXTH }`. Keep only the constants that are actually used: `ONE`, `ZERO`, `ONE_THIRD`. The corrected import should be: `import { ONE, ZERO, ONE_THIRD } from '@/core/utils/fraction'`

4. In `src/core/faraid/__tests__/special.test.ts` line 2: Remove the entire `import Fraction from 'fraction.js'` line. The test file does not use `Fraction` directly.

After making these changes:
- Run `bun run vitest run --reporter=verbose` to confirm all 158 tests still pass.
- Run `bun run build` to confirm zero TypeScript errors and `dist/index.html` is produced.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && bun run vitest run --reporter=verbose && bun run build && test -f dist/index.html && echo "BUILD OK"</automated>
  </verify>
  <done>All 6 unused-import errors are removed. `bun run build` exits with code 0 and produces `dist/index.html`. All 158 tests still pass. DSGN-04 is fully satisfied.</done>
</task>

</tasks>

<verification>
1. `bun run build` exits with code 0 (no TypeScript errors)
2. `dist/index.html` exists with a timestamp newer than all source files
3. `bun run vitest run` reports 158 tests passing, 0 failures
4. No remaining `import.*unused` patterns in the 3 modified files
</verification>

<success_criteria>
- Zero TypeScript compiler errors on `bun run build`
- `dist/index.html` produced by the build
- All 158 existing tests pass without modification to test logic
- Phase 1 success criterion 5 ("The project builds, passes linting, and deploys as a static site to Netlify") is now fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-faraid-engine-and-project-foundation/01-04-SUMMARY.md`
</output>

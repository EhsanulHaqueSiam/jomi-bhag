---
phase: 17-dead-code-cleanup-and-documentation-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/division/LotDivisionPage.tsx
  - src/components/division/GroupCard.tsx
  - src/components/division/ParcelRow.tsx
  - src/components/division/QurahReference.tsx
  - src/stores/divisionStore.ts
  - src/components/wizard/StepFamily.tsx
  - src/components/wizard/StepSiblings.tsx
  - src/components/__tests__/division.test.tsx
  - src/components/pdf/PdfLotDivisionSection.tsx
  - src/App.tsx
  - src/types/scenario.ts
  - src/components/layout/AppLayout.tsx
  - src/hooks/usePdfExport.tsx
  - src/hooks/__tests__/usePdfExport.test.ts
  - src/components/pdf/extractPdfData.ts
  - src/components/pdf/pdfTypes.ts
  - src/components/pdf/PdfDocument.tsx
  - src/components/results/MonetaryBarChart.tsx
  - src/core/distribution/types.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - P9-SC3
gap_closure: true

must_haves:
  truths:
    - "No dead imports or unreachable code paths remain after Phase 11 superseded Phase 9 and Quick-8 merged wizard steps"
    - "MonetaryBarChart renders graded emerald coloring per bar matching pie chart visual style"
    - "REQUIREMENTS.md traceability accurately reflects completion status of all Phase 9-14 requirements"
    - "All existing tests pass and TypeScript compiles cleanly after deletions"
  artifacts:
    - path: "src/components/division/LotDivisionPage.tsx"
      provides: "DELETED -- dead page component"
    - path: "src/components/division/GroupCard.tsx"
      provides: "DELETED -- only used by LotDivisionPage"
    - path: "src/components/division/ParcelRow.tsx"
      provides: "DELETED -- only used by GroupCard"
    - path: "src/components/division/QurahReference.tsx"
      provides: "DELETED -- only used by LotDivisionPage"
    - path: "src/stores/divisionStore.ts"
      provides: "DELETED -- only used by LotDivisionPage and usePdfExport"
    - path: "src/components/wizard/StepFamily.tsx"
      provides: "DELETED -- replaced by StepFamilyAndSiblings"
    - path: "src/components/wizard/StepSiblings.tsx"
      provides: "DELETED -- replaced by StepFamilyAndSiblings"
    - path: "src/components/__tests__/division.test.tsx"
      provides: "DELETED -- tests dead LotDivisionPage components"
    - path: "src/components/pdf/PdfLotDivisionSection.tsx"
      provides: "DELETED -- dead PDF section for lot division"
    - path: "src/components/results/MonetaryBarChart.tsx"
      provides: "Per-bar emerald coloring via Recharts Cell"
      contains: "Cell"
  key_links:
    - from: "src/App.tsx"
      to: "LotDivisionPage"
      via: "REMOVED -- division route deleted"
      pattern: "division.*removed"
    - from: "src/hooks/usePdfExport.tsx"
      to: "divisionStore"
      via: "REMOVED -- divisionStore import deleted"
      pattern: "divisionStore.*removed"
    - from: "src/components/results/MonetaryBarChart.tsx"
      to: "EMERALD_COLORS"
      via: "Cell components inside Bar"
      pattern: "EMERALD_COLORS.*Cell"
---

<objective>
Remove all dead code left by Phase 11 superseding Phase 9 (LotDivisionPage, divisionStore, GroupCard, ParcelRow, QurahReference, PdfLotDivisionSection) and Quick-8 merging wizard steps (StepFamily, StepSiblings), clean all dangling import references, fix MonetaryBarChart per-bar coloring, remove stale comment, and update REQUIREMENTS.md traceability.

Purpose: Clean codebase of unreachable code, fix minor visual bug, accurate documentation.
Output: 9 files deleted, ~10 files edited, all tests green, tsc clean.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dead-code-cleanup-and-documentation-fix/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead files and clean all import references</name>
  <files>
    src/components/division/LotDivisionPage.tsx (DELETE)
    src/components/division/GroupCard.tsx (DELETE)
    src/components/division/ParcelRow.tsx (DELETE)
    src/components/division/QurahReference.tsx (DELETE)
    src/stores/divisionStore.ts (DELETE)
    src/components/wizard/StepFamily.tsx (DELETE)
    src/components/wizard/StepSiblings.tsx (DELETE)
    src/components/__tests__/division.test.tsx (DELETE)
    src/components/pdf/PdfLotDivisionSection.tsx (DELETE)
    src/App.tsx
    src/types/scenario.ts
    src/components/layout/AppLayout.tsx
    src/hooks/usePdfExport.tsx
    src/hooks/__tests__/usePdfExport.test.ts
    src/components/pdf/extractPdfData.ts
    src/components/pdf/pdfTypes.ts
    src/components/pdf/PdfDocument.tsx
  </files>
  <action>
    Full cascade removal of dead code per user decision.

    **Step 1 -- Delete 9 dead files:**
    - `src/components/division/LotDivisionPage.tsx`
    - `src/components/division/GroupCard.tsx`
    - `src/components/division/ParcelRow.tsx`
    - `src/components/division/QurahReference.tsx`
    - `src/stores/divisionStore.ts`
    - `src/components/wizard/StepFamily.tsx`
    - `src/components/wizard/StepSiblings.tsx`
    - `src/components/__tests__/division.test.tsx`
    - `src/components/pdf/PdfLotDivisionSection.tsx`

    CRITICAL: Do NOT delete these files in the division/ directory -- they are LIVE:
    - `CompensationBanner.tsx` (imported by DistributionPage.tsx)
    - `QurahCeremony.tsx` (imported by IndivisibleCard.tsx)
    And do NOT delete `src/core/land/__tests__/division.test.ts` (tests live algorithm).

    **Step 2 -- Clean import references in surviving files:**

    `src/App.tsx`:
    - Remove `import { LotDivisionPage }` from `@/components/division/LotDivisionPage`
    - Remove the `{page === 'division' && <LotDivisionPage onNavigate={setPage} />}` JSX block

    `src/types/scenario.ts`:
    - Remove `'division'` from the `AppPage` type union (result: `'wizard' | 'scenarios' | 'distribution'`)

    `src/components/layout/AppLayout.tsx`:
    - Change `const isWide = page === 'distribution' || page === 'division'` to `const isWide = page === 'distribution'`

    `src/hooks/usePdfExport.tsx`:
    - Remove the line that dynamically imports divisionStore: `const divisionState = (await import('@/stores/divisionStore')).useDivisionStore.getState()`
    - Remove the `const divisionResult = divisionState.divisionResult` line
    - Remove `divisionResult` from the `extractPdfData(...)` call arguments

    `src/hooks/__tests__/usePdfExport.test.ts`:
    - Remove the `vi.mock('@/stores/divisionStore', ...)` block
    - Remove any divisionResult references in test expectations

    `src/components/pdf/extractPdfData.ts`:
    - Remove the `divisionResult?: DivisionResult | null` parameter
    - Remove `import type { DivisionResult } from '@/core/land/division'`
    - Remove `PdfLotDivision` from the pdfTypes import
    - Remove the entire lotDivision mapping block (the block that maps divisionResult to PdfLotDivision format)
    - Remove `lotDivision` from the return object
    - If there is a `lotDivision = undefined` assignment in the distribution block, remove it

    `src/components/pdf/pdfTypes.ts`:
    - Remove the `PdfLotDivision` type definition
    - Remove the `PdfLotDivisionGroup` type definition
    - Remove the `lotDivision?: PdfLotDivision` field from the `PdfData` interface

    `src/components/pdf/PdfDocument.tsx`:
    - Remove `import { PdfLotDivisionSection }` from the imports
    - Remove the conditional rendering of `PdfLotDivisionSection`

    Also search for and update `src/components/__tests__/pdf-distribution.test.tsx` -- it calls `extractPdfData` with a `divisionResult` argument that must be removed (the `makeDivisionResult()` calls). Remove the `makeDivisionResult` factory function if it exists only for this purpose.

    **Step 3 -- Verify no breakage:**
    Run `npx tsc -b --noEmit` and `npx vitest run` to confirm zero compilation errors and all tests pass.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 9 dead files deleted, all import references cleaned from surviving files, TypeScript compiles cleanly, all existing tests pass (test count reduced by the number of tests in the deleted division.test.tsx file).</done>
</task>

<task type="auto">
  <name>Task 2: Fix bar chart coloring, remove stale comment, update REQUIREMENTS.md</name>
  <files>
    src/components/results/MonetaryBarChart.tsx
    src/core/distribution/types.ts
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Three independent fixes:

    **Fix 1 -- MonetaryBarChart per-bar emerald coloring (per user decision):**

    In `src/components/results/MonetaryBarChart.tsx`:
    - Add `Cell` to the Recharts import: `import { ..., Cell } from 'recharts'`
    - Import EMERALD_COLORS: `import { EMERALD_COLORS } from '@/components/results/chartData'`
    - Inside the `<Bar>` element, add Cell children that cycle through EMERALD_COLORS:
      ```
      <Bar dataKey="bdtValue" radius={[0, 4, 4, 0]} barSize={28}>
        {chartData.map((entry, index) => (
          <Cell key={entry.name} fill={EMERALD_COLORS[index % EMERALD_COLORS.length]} />
        ))}
      </Bar>
      ```
    - The chartData variable name may differ -- use whatever the component passes to BarChart's `data` prop.

    **Fix 2 -- Remove stale comment:**

    In `src/core/distribution/types.ts`:
    - Remove the first line if it reads `// Stub - types to be implemented` (or similar stale stub comment).
    - The file should start with the actual imports or type definitions.

    **Fix 3 -- REQUIREMENTS.md traceability (verify and fix if needed):**

    Read `.planning/REQUIREMENTS.md` and verify:
    - All Phase 9-14 requirement rows in the Traceability table show status "Complete"
    - All requirement checkboxes `[x]` are checked for Phase 9-14 items
    - If any show "Planned" or `[ ]`, update them to "Complete" / `[x]`
    - The research notes this may already be correct -- verify before editing.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/charts.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>MonetaryBarChart renders graded emerald colors per bar (matching pie chart style). Stale "stub" comment removed from distribution types.ts. REQUIREMENTS.md traceability accurately reflects all Phase 9-14 requirements as Complete.</done>
</task>

</tasks>

<verification>
1. `npx tsc -b --noEmit` -- zero TypeScript compilation errors
2. `npx vitest run` -- all tests pass (test count reduced by deleted division.test.tsx tests)
3. Deleted files no longer exist: `ls src/components/division/LotDivisionPage.tsx src/stores/divisionStore.ts src/components/wizard/StepFamily.tsx src/components/wizard/StepSiblings.tsx 2>&1` should show "No such file"
4. Live division files still exist: `ls src/components/division/CompensationBanner.tsx src/components/division/QurahCeremony.tsx src/core/land/division.ts 2>&1` should show them
5. MonetaryBarChart contains Cell import: `grep 'Cell' src/components/results/MonetaryBarChart.tsx`
6. No stale stub comment: `head -1 src/core/distribution/types.ts` should NOT show "Stub"
</verification>

<success_criteria>
- Zero dead files remain (9 files deleted)
- Zero dangling imports (tsc clean)
- All existing tests pass (vitest green)
- MonetaryBarChart uses graded emerald Cell coloring
- distribution/types.ts has no stale stub comment
- REQUIREMENTS.md traceability is accurate for Phase 9-14
</success_criteria>

<output>
After completion, create `.planning/phases/17-dead-code-cleanup-and-documentation-fix/17-01-SUMMARY.md`
</output>

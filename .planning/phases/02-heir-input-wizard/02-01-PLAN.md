---
phase: 02-heir-input-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/wizard.ts
  - src/stores/wizardStore.ts
  - src/stores/__tests__/wizardStore.test.ts
  - src/index.css
  - package.json
autonomous: true
requirements: [HEIR-01, HEIR-02, HEIR-03, HEIR-04, HEIR-05, DSGN-01]

must_haves:
  truths:
    - "Zustand store holds all wizard state (step, relationship, heir counts, MFLO toggle)"
    - "Selecting a relationship derives deceased gender and user gender correctly"
    - "Auto-include logic adds the user as heir based on relationship selection"
    - "Auto-include logic adds mother-as-wife when user picks 'my Father' and mother is alive"
    - "Changing relationship resets auto-included heirs from previous selection"
    - "buildFaraidInput() produces a valid FaraidInput with no zero-count heirs"
    - "Progressive disclosure toggle preserves sibling sub-type counts in store"
    - "Design system defines emerald green primary, gold accent, and Inter + Noto Naskh Arabic fonts"
  artifacts:
    - path: "src/types/wizard.ts"
      provides: "WizardState, RelationshipType, step config types"
      exports: ["RelationshipType", "WizardStep", "WizardState"]
    - path: "src/stores/wizardStore.ts"
      provides: "Zustand store with all wizard state, actions, and buildFaraidInput"
      exports: ["useWizardStore"]
    - path: "src/stores/__tests__/wizardStore.test.ts"
      provides: "Unit tests for store logic, auto-includes, FaraidInput building"
      min_lines: 100
    - path: "src/index.css"
      provides: "TailwindCSS 4 @theme with emerald/gold palette, font families"
      contains: "@theme"
  key_links:
    - from: "src/stores/wizardStore.ts"
      to: "src/core/faraid/types.ts"
      via: "import FaraidInput, HeirInput, HeirType"
      pattern: "import type.*FaraidInput"
    - from: "src/stores/wizardStore.ts"
      to: "src/types/wizard.ts"
      via: "import RelationshipType, WizardState"
      pattern: "import.*RelationshipType"
---

<objective>
Create the wizard's data foundation: Zustand store with complete state logic, wizard types, and TailwindCSS 4 design system.

Purpose: The store is the heart of the wizard -- it holds all state, derives deceased gender from relationship selection, manages auto-include logic, validates per-step, and builds the FaraidInput object that feeds the Phase 1 engine. The design system establishes the emerald green + gold visual identity consumed by all UI components.

Output: Tested Zustand store, wizard type definitions, and design system tokens in index.css.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-heir-input-wizard/02-CONTEXT.md
@.planning/phases/02-heir-input-wizard/02-RESEARCH.md

<interfaces>
<!-- Key types from Phase 1 engine that the wizard store must produce -->

From src/core/faraid/types.ts:
```typescript
export type HeirType =
  | 'husband' | 'wife' | 'son' | 'daughter'
  | 'son_of_son' | 'daughter_of_son'
  | 'father' | 'paternal_grandfather'
  | 'mother' | 'paternal_grandmother' | 'maternal_grandmother'
  | 'brother_full' | 'brother_consanguine' | 'brother_uterine'
  | 'sister_full' | 'sister_consanguine' | 'sister_uterine'

export interface HeirInput {
  type: HeirType
  count: number
}

export interface FaraidInput {
  deceasedGender: 'male' | 'female'
  heirs: HeirInput[]
  mfloEnabled?: boolean
  predeceasedChildren?: PredeceasedChild[]
}
```

From src/core/faraid/validation.ts:
```typescript
export function validateHeirInput(input: FaraidInput): ValidationResult
export interface ValidationResult { valid: boolean; errors: string[] }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, create wizard types, and design system</name>
  <files>package.json, src/types/wizard.ts, src/index.css</files>
  <behavior>
    - deriveDeceasedGender('father') returns 'male'
    - deriveDeceasedGender('mother') returns 'female'
    - deriveDeceasedGender('husband') returns 'female' (user is male, deceased is female)
    - deriveDeceasedGender('wife') returns 'male' (user is female, deceased is male)
    - deriveDeceasedGender('brother') returns 'male'
    - deriveDeceasedGender('sister') returns 'female'
    - deriveDeceasedGender('other') returns null
    - deriveUserGender('husband') returns 'male'
    - deriveUserGender('wife') returns 'female'
    - deriveUserGender('father') returns null (ambiguous -- could be son or daughter)
  </behavior>
  <action>
    1. Install zustand and motion: `bun add zustand motion`

    2. Create `src/types/wizard.ts` with:
       - `RelationshipType = 'father' | 'mother' | 'husband' | 'wife' | 'brother' | 'sister' | 'other'`
       - `WizardStep` interface: `{ number: number, label: string, shortLabel: string }`
       - `WIZARD_STEPS` constant array: step 1 "Relationship", step 2 "Family", step 3 "Siblings"
       - `deriveDeceasedGender(rel: RelationshipType): 'male' | 'female' | null` -- pure function
       - `deriveUserGender(rel: RelationshipType): 'male' | 'female' | null` -- pure function
       - `getAutoIncludes(relationship, userGender, motherAlive): { type: HeirType, count: number }[]` -- pure function per RESEARCH.md auto-include logic
       - The WizardState interface (full shape per RESEARCH.md Pattern 1) -- BUT export it from this types file so both store and tests can import it

    3. Update `src/index.css` to add TailwindCSS 4 @theme block:
       - Keep existing `@import "tailwindcss"` and Noto Naskh Arabic import
       - Add Inter font import from Google Fonts
       - Add `@theme` block with:
         - `--font-sans: 'Inter', ui-sans-serif, system-ui, sans-serif`
         - `--font-arabic: 'Noto Naskh Arabic', serif`
         - Gold accent colors: `--color-gold-50` through `--color-gold-600` using oklch values per RESEARCH.md
         - Step transition keyframe animation
       - Emerald palette is built into TailwindCSS 4 -- do NOT redefine it. Only define gold custom colors.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && bun run build</automated>
  </verify>
  <done>zustand and motion installed, wizard types exported with pure derivation functions, index.css has @theme with gold palette and font families, project builds cleanly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Zustand wizard store with unit tests</name>
  <files>src/stores/wizardStore.ts, src/stores/__tests__/wizardStore.test.ts</files>
  <behavior>
    - Store initializes with currentStep=1, relationship=null, all counts at 0
    - setRelationship('father') sets deceasedGender='male', resets auto-includes
    - setRelationship('father') then setRelationship('wife') clears old auto-includes and sets new ones
    - When relationship='father' and userGender='male' and motherAlive=true: auto-includes are [{type:'son',count:1}, {type:'wife',count:1}]
    - When relationship='husband': auto-includes are [{type:'husband',count:1}]
    - When relationship='other': auto-includes are empty, deceasedGender=null
    - setStep(2) updates currentStep to 2 and adds 1 to completedSteps
    - buildFaraidInput() filters zero-count heirs, maps wizard state to HeirInput[]
    - buildFaraidInput() when siblingTypeExpanded=false treats all brothers as brother_full
    - buildFaraidInput() when siblingTypeExpanded=true preserves individual sibling sub-types
    - buildFaraidInput() includes mfloEnabled flag
    - Toggling siblingTypeExpanded does NOT reset sibling counts
    - wifeCount max is 4, sonCount/daughterCount min is 0
    - isStepValid(1) returns true only when relationship is selected (and deceasedGender is set for 'other')
    - isStepValid(2) returns true always (zero children/spouse is valid -- maybe no spouse)
    - isStepValid(3) returns true always (zero siblings is valid)
  </behavior>
  <action>
    Create `src/stores/wizardStore.ts`:
    - Import `create` from 'zustand'
    - Import types from `@/types/wizard` (RelationshipType, WizardState, deriveDeceasedGender, deriveUserGender, getAutoIncludes)
    - Import FaraidInput, HeirInput from `@/core/faraid/types`
    - Implement the complete store per RESEARCH.md Pattern 1 with these actions:
      - `setStep(step)`: Sets currentStep, marks previous step as completed
      - `setRelationship(rel)`: Derives deceasedGender via deriveDeceasedGender(), derives userGender via deriveUserGender(), resets motherAlive to null, resets all auto-included heir counts, stores new relationship
      - `setUserGender(gender)`: For disambiguating "my Father"/"my Mother" cases
      - `setMotherAlive(alive)`: Sets motherAlive, triggers auto-include recalculation
      - `setDeceasedGender(gender)`: For "Other" relationship explicit selection
      - `setMfloEnabled(enabled)`: Toggle MFLO
      - `setWifeCount(n)`, `setHusbandPresent(b)`, `setSonCount(n)`, `setDaughterCount(n)`: Step 2 setters with bounds checking (wife max 4)
      - `setBrotherFullCount(n)`, `setBrotherConsanguineCount(n)`, `setBrotherUterineCount(n)`: Step 3 setters
      - `setSisterFullCount(n)`, `setSisterConsanguineCount(n)`, `setSisterUterineCount(n)`: Step 3 setters
      - `setSiblingTypeExpanded(expanded)`: Toggle progressive disclosure (does NOT reset counts)
      - `isStepValid(step)`: Returns boolean for per-step validation
      - `buildFaraidInput()`: Builds FaraidInput from current state. Filters zero-count heirs. When siblingTypeExpanded=false, sums all brother counts as brother_full and all sister counts as sister_full. Uses getAutoIncludes() to merge auto-included heirs with manual counts (auto-includes are additive -- if user is auto-added as 1 son and manually enters 2 more sons, total is 3).

    IMPORTANT implementation details:
    - Auto-includes should be tracked as separate state (e.g., `autoIncludes: { type: HeirType, count: number }[]`) so they can be cleanly replaced when relationship changes per RESEARCH.md Pitfall 1.
    - buildFaraidInput merges autoIncludes with manual heir counts. For example, if autoIncludes has {son:1} and user manually set sonCount=2, the merged HeirInput for 'son' should have count=3.
    - When relationship changes, reset: autoIncludes, motherAlive, wifeCount, husbandPresent (since spouse type depends on deceased gender). Do NOT reset sonCount/daughterCount/sibling counts -- user may have entered those already.
    - completedSteps stored as number[] (not Set -- Zustand serialization). Use Array.includes() to check membership.

    Create `src/stores/__tests__/wizardStore.test.ts`:
    - Import useWizardStore
    - Use `useWizardStore.getState()` and `useWizardStore.setState()` for direct store testing (no component rendering needed)
    - Reset store state in beforeEach
    - Test groups:
      - "relationship derivation": Test all 7 relationship types produce correct deceasedGender
      - "auto-includes": Test father+male user, father+female user, mother, husband, wife, brother, sister, other
      - "auto-include cleanup on relationship change": Set father, verify auto-includes, change to wife, verify old auto-includes cleared
      - "mother alive auto-include": Set father, set motherAlive=true, verify wife auto-included
      - "buildFaraidInput": Test with various heir combinations, verify no zero-count heirs, verify collapsed sibling default
      - "buildFaraidInput with expanded siblings": Verify sub-types preserved
      - "step validation": Test isStepValid for each step
      - "progressive disclosure preserves counts": Set expanded, enter consanguine brothers, collapse, verify count still in store
      - "parents excluded": Verify no father/mother/grandfather/grandmother types can be added through the wizard (HEIR-05)
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && bun vitest run src/stores/__tests__/wizardStore.test.ts</automated>
  </verify>
  <done>Zustand store passes all unit tests: relationship derivation correct for all 7 types, auto-includes work for all relationship paths, buildFaraidInput produces valid FaraidInput, step validation works, progressive disclosure preserves data</done>
</task>

</tasks>

<verification>
- `bun run test:run` -- all tests pass (existing Phase 1 + new store tests)
- `bun run build` -- TypeScript compiles cleanly with no errors
- `src/stores/wizardStore.ts` exports `useWizardStore` with `buildFaraidInput()` method
- `src/types/wizard.ts` exports `RelationshipType`, `deriveDeceasedGender`, `deriveUserGender`, `getAutoIncludes`
- `src/index.css` contains `@theme` block with gold colors and font families
</verification>

<success_criteria>
- Store unit tests pass covering all relationship types, auto-include scenarios, and FaraidInput building
- buildFaraidInput() output validated against FaraidInput type (no type errors)
- Design system tokens available via TailwindCSS utility classes (text-gold-500, font-arabic, etc.)
- Zero TypeScript build errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-heir-input-wizard/02-01-SUMMARY.md`
</output>

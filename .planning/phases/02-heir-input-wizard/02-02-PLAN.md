---
phase: 02-heir-input-wizard
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/ui/StepperButton.tsx
  - src/components/ui/StepIndicator.tsx
  - src/components/ui/Tooltip.tsx
  - src/components/ui/Button.tsx
  - src/components/layout/AppLayout.tsx
  - src/components/wizard/WizardShell.tsx
  - src/components/wizard/steps.ts
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-03]

must_haves:
  truths:
    - "StepperButton renders [ - ] count [ + ] with min/max enforcement and disabled states"
    - "StepIndicator shows 3 connected dots with active/completed/upcoming visual states"
    - "StepIndicator dots are clickable for completed steps (free navigation)"
    - "StepIndicator shrinks to compact dots on mobile (<768px)"
    - "WizardShell renders current step with direction-aware slide animation via motion"
    - "AppLayout renders centered card on desktop, full-width on mobile"
    - "Tooltip shows (?) icon that reveals explanation text on hover/tap"
    - "Next/Back buttons are full-width bottom bar on mobile, inline on desktop"
  artifacts:
    - path: "src/components/ui/StepperButton.tsx"
      provides: "Reusable [ - ] count [ + ] control with label, min, max, tooltip"
      exports: ["StepperButton"]
    - path: "src/components/ui/StepIndicator.tsx"
      provides: "Connected-dots progress indicator with clickable completed steps"
      exports: ["StepIndicator"]
    - path: "src/components/ui/Tooltip.tsx"
      provides: "(?) icon with hover/tap popover explanation"
      exports: ["Tooltip"]
    - path: "src/components/ui/Button.tsx"
      provides: "Primary/secondary button with emerald green styling"
      exports: ["Button"]
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Page shell with header (Jomi-Bhag branding), centered card container"
      exports: ["AppLayout"]
    - path: "src/components/wizard/WizardShell.tsx"
      provides: "Wizard container: step indicator + animated step content + navigation buttons"
      exports: ["WizardShell"]
    - path: "src/components/wizard/steps.ts"
      provides: "Step configuration array mapping step numbers to labels and components"
      exports: ["WIZARD_STEPS"]
  key_links:
    - from: "src/components/wizard/WizardShell.tsx"
      to: "src/stores/wizardStore.ts"
      via: "useWizardStore() for currentStep, completedSteps, setStep, isStepValid"
      pattern: "useWizardStore"
    - from: "src/components/ui/StepIndicator.tsx"
      to: "src/stores/wizardStore.ts"
      via: "useWizardStore() for step state and navigation"
      pattern: "useWizardStore"
    - from: "src/components/wizard/WizardShell.tsx"
      to: "motion/react"
      via: "AnimatePresence + motion.div for step transitions"
      pattern: "AnimatePresence"
---

<objective>
Build all UI primitives and the wizard shell that renders step content with animated transitions and responsive navigation.

Purpose: These components are the visual building blocks consumed by the step components (Plan 03). The WizardShell orchestrates step display, navigation, and animations. The UI primitives (StepperButton, StepIndicator, Tooltip, Button) are reusable across all steps and future phases.

Output: Complete set of UI components and wizard shell, ready for step content components to be plugged in.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-heir-input-wizard/02-CONTEXT.md
@.planning/phases/02-heir-input-wizard/02-RESEARCH.md
@.planning/phases/02-heir-input-wizard/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/types/wizard.ts (created in Plan 01):
```typescript
export type RelationshipType = 'father' | 'mother' | 'husband' | 'wife' | 'brother' | 'sister' | 'other'

export interface WizardStep {
  number: number
  label: string
  shortLabel: string
}

export const WIZARD_STEPS: WizardStep[]
```

From src/stores/wizardStore.ts (created in Plan 01):
```typescript
export const useWizardStore: UseBoundStore<StoreApi<WizardState>>
// Key selectors used by UI components:
// - currentStep: number
// - completedSteps: number[]
// - setStep(step: number): void
// - isStepValid(step: number): boolean
// - relationship: RelationshipType | null
// - deceasedGender: 'male' | 'female' | null
```

From src/index.css (updated in Plan 01):
```css
/* Available TailwindCSS classes:
   - text-emerald-{50-950} (built-in)
   - bg-emerald-{50-950} (built-in)
   - text-gold-{50-600} (custom @theme)
   - bg-gold-{50-600} (custom @theme)
   - font-sans (Inter)
   - font-arabic (Noto Naskh Arabic)
*/
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI primitives (StepperButton, StepIndicator, Tooltip, Button)</name>
  <files>src/components/ui/StepperButton.tsx, src/components/ui/StepIndicator.tsx, src/components/ui/Tooltip.tsx, src/components/ui/Button.tsx</files>
  <action>
    Create 4 reusable UI components following the emerald green design system and mobile-first responsive approach.

    **StepperButton.tsx** (per RESEARCH.md Pattern 2):
    - Props: `{ label: string, value: number, min?: number (default 0), max?: number (default 99), onChange: (value: number) => void, tooltip?: string, disabled?: boolean }`
    - Layout: label+tooltip on left, [ - ] count [ + ] on right, in a flex row with `justify-between`
    - Decrement/increment buttons: `w-10 h-10` (44px tap target for mobile), rounded-full, emerald-300 border, emerald-600 text, disabled when at min/max with opacity-30
    - Count display: centered number between buttons, text-lg font-semibold
    - Enforce min/max in onChange handler (Math.min/Math.max) per RESEARCH.md Pitfall 2
    - If tooltip prop provided, render Tooltip component next to label

    **StepIndicator.tsx** (per RESEARCH.md Pattern 4):
    - Props: none (reads from useWizardStore directly: currentStep, completedSteps, setStep, isStepValid)
    - Desktop (md:+): 3 numbered circles connected by lines. Labels below circles.
    - Mobile (<md): 3 smaller dots (w-8 h-8) with no labels, just numbers. Connected by shorter lines.
    - Active step: bg-emerald-600 text-white shadow-lg
    - Completed step (not active): bg-emerald-100 text-emerald-700, shows checkmark (unicode ✓), clickable via `onClick={() => setStep(step.number)}`
    - Upcoming step: bg-gray-100 text-gray-400, NOT clickable
    - Connector lines: h-0.5, emerald-400 if preceding step completed, gray-200 otherwise. `w-12 md:w-20`
    - Import WIZARD_STEPS from `@/types/wizard` for step labels

    **Tooltip.tsx**:
    - Props: `{ content: string }`
    - Renders a (?) icon: `w-5 h-5` inline circle with question mark, text-gray-400 hover:text-emerald-600
    - On hover (desktop) or tap (mobile): shows a popover with the content string
    - Implementation: Use a `useState` boolean for visibility. Position the popover with `absolute` relative to the icon. Arrow pointing to icon. White background, shadow-lg, rounded-lg, max-w-xs, text-sm text-gray-600, p-3.
    - Close on click outside: add a click handler on document when open, remove on close.
    - Use `role="tooltip"` and `aria-describedby` for accessibility.

    **Button.tsx**:
    - Props: `{ children: React.ReactNode, variant?: 'primary' | 'secondary' | 'ghost', onClick?: () => void, disabled?: boolean, fullWidth?: boolean, className?: string, type?: 'button' | 'submit' }`
    - Primary: bg-emerald-600 text-white hover:bg-emerald-700 active:bg-emerald-800, disabled:opacity-50
    - Secondary: bg-white text-emerald-700 border border-emerald-300 hover:bg-emerald-50
    - Ghost: text-gray-500 hover:text-emerald-600 hover:bg-gray-50
    - All variants: rounded-xl px-6 py-3 font-medium transition-colors
    - fullWidth: adds `w-full`
    - Large tap targets: minimum h-12 for mobile usability
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && bun run build</automated>
  </verify>
  <done>All 4 UI components export correctly, TypeScript compiles, emerald styling applied per design system</done>
</task>

<task type="auto">
  <name>Task 2: Create AppLayout, WizardShell with animated transitions, and step config</name>
  <files>src/components/layout/AppLayout.tsx, src/components/wizard/WizardShell.tsx, src/components/wizard/steps.ts</files>
  <action>
    **steps.ts**:
    - Re-export WIZARD_STEPS from `@/types/wizard` for convenience
    - This file exists as the canonical import point for wizard step configuration within the wizard/ directory

    **AppLayout.tsx** (per RESEARCH.md responsive layout pattern):
    - Props: `{ children: React.ReactNode }`
    - Outer wrapper: `min-h-screen bg-gray-50`
    - Header: centered "Jomi-Bhag" title (text-2xl md:text-3xl font-bold text-emerald-800) + "Islamic Inheritance Calculator" subtitle (text-sm text-gray-500). Padding: `px-4 py-6 md:py-8`
    - Optional: subtle Islamic geometric pattern as background decoration. Use a simple SVG pattern in a `before` pseudo-element or a background div with very low opacity (0.03-0.05). If too complex, skip -- the emerald + white palette carries the design. Per CONTEXT.md: "subtle Islamic geometric patterns"
    - Main content area: `px-4 pb-24 md:pb-8 md:max-w-lg lg:max-w-xl md:mx-auto`
    - Card container: `bg-white rounded-2xl shadow-sm border border-gray-100 p-4 md:p-8`
    - The `pb-24` on mobile reserves space for the fixed bottom navigation bar

    **WizardShell.tsx** (per RESEARCH.md Pattern 3):
    - Reads from useWizardStore: currentStep, completedSteps, setStep, isStepValid
    - Tracks `direction` state (number): +1 for forward, -1 for backward
    - Layout structure:
      1. StepIndicator at top
      2. AnimatePresence wrapper with step content area
      3. Navigation bar at bottom
    - Step content area: Uses `AnimatePresence mode="wait"` with `motion.div` keyed on currentStep
      - Variants per RESEARCH.md: enter slides in from direction side, center is x:0 opacity:1, exit slides out opposite
      - Transition: duration 0.3s, easeInOut
      - For now, render placeholder `<div>` for each step with text "Step {n} content" since step components come in Plan 03
    - Navigation bar:
      - Mobile (<md): Fixed bottom bar (`fixed bottom-0 left-0 right-0 bg-white border-t border-gray-100 px-4 py-3 flex gap-3`). Full-width buttons.
      - Desktop (md:+): Inline flex row at bottom of card (`flex justify-between mt-8`)
      - Back button (secondary variant): visible on steps 2 and 3. Updates direction to -1 then calls setStep(currentStep - 1).
      - Next button (primary variant): visible on steps 1 and 2. Disabled when isStepValid(currentStep) is false. Updates direction to +1 then calls setStep(currentStep + 1).
      - Calculate button (primary variant): visible on step 3 only. Text: "Calculate Shares". Disabled when isStepValid(3) is false. For now, onClick logs to console -- Phase 3 will wire this to results.
    - Parents-deceased info text: On the WizardShell (visible across all steps), show a subtle banner: "This calculator assumes the deceased's parents have passed away" per CONTEXT.md. Style: text-xs text-gray-400, below the StepIndicator, centered. Use an info icon (i) before the text.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && bun run build</automated>
  </verify>
  <done>AppLayout renders header + card container, WizardShell displays step indicator + placeholder step content with slide animations + responsive navigation bar, parents-deceased info text visible</done>
</task>

</tasks>

<verification>
- `bun run build` -- TypeScript compiles with zero errors
- `bun run test:run` -- all existing tests still pass
- All components export correctly from their respective files
- StepperButton enforces min/max bounds
- StepIndicator reads from Zustand store and renders 3 connected dots
- WizardShell uses AnimatePresence with direction-aware transitions
- AppLayout responsive: full-width mobile, centered card desktop
</verification>

<success_criteria>
- All 7 component files created and exporting correctly
- Project builds with `bun run build` (zero TypeScript errors)
- UI primitives use emerald green design system consistently
- WizardShell has working animated step transitions via motion
- Navigation buttons respect step validation (Next disabled when invalid)
- Mobile layout uses full-width stacked approach with fixed bottom nav
</success_criteria>

<output>
After completion, create `.planning/phases/02-heir-input-wizard/02-02-SUMMARY.md`
</output>

---
phase: 02-heir-input-wizard
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/components/wizard/StepRelationship.tsx
  - src/components/wizard/StepFamily.tsx
  - src/components/wizard/StepSiblings.tsx
  - src/components/wizard/WizardShell.tsx
  - src/App.tsx
  - src/components/__tests__/wizard.test.tsx
autonomous: false
requirements: [HEIR-01, HEIR-02, HEIR-03, HEIR-04, HEIR-05, DSGN-01, DSGN-02, DSGN-03]

must_haves:
  truths:
    - "User selects relationship to deceased from 7 options and deceased gender is derived"
    - "User selecting 'my Father' is prompted for their gender (son/daughter) and whether mother is alive"
    - "Step 2 shows conditional spouse field: Wives stepper (1-4) for male deceased, married checkbox for female deceased"
    - "Step 2 shows son and daughter stepper buttons"
    - "Step 3 shows Brothers and Sisters steppers with progressive disclosure toggle"
    - "Expanding sibling types shows full/consanguine/uterine sub-type steppers"
    - "Collapsing sibling types preserves sub-type counts in store"
    - "MFLO toggle appears under Advanced section on Step 1"
    - "Tooltips explain Islamic terms like 'consanguine' on demand"
    - "App renders the complete wizard on page load -- no landing page"
    - "The wizard works on 375px mobile viewport and 1024px+ desktop"
  artifacts:
    - path: "src/components/wizard/StepRelationship.tsx"
      provides: "Step 1: relationship selector, user gender disambiguation, mother-alive prompt, MFLO toggle"
      exports: ["StepRelationship"]
    - path: "src/components/wizard/StepFamily.tsx"
      provides: "Step 2: conditional spouse entry, son/daughter steppers"
      exports: ["StepFamily"]
    - path: "src/components/wizard/StepSiblings.tsx"
      provides: "Step 3: brother/sister steppers with progressive disclosure"
      exports: ["StepSiblings"]
    - path: "src/App.tsx"
      provides: "Root component rendering AppLayout > WizardShell"
      exports: ["default"]
    - path: "src/components/__tests__/wizard.test.tsx"
      provides: "Component tests for wizard navigation and step rendering"
      min_lines: 50
  key_links:
    - from: "src/components/wizard/StepRelationship.tsx"
      to: "src/stores/wizardStore.ts"
      via: "useWizardStore for setRelationship, setUserGender, setMotherAlive, setMfloEnabled"
      pattern: "useWizardStore"
    - from: "src/components/wizard/StepFamily.tsx"
      to: "src/stores/wizardStore.ts"
      via: "useWizardStore for spouse/children counts and deceased gender"
      pattern: "useWizardStore"
    - from: "src/components/wizard/StepSiblings.tsx"
      to: "src/stores/wizardStore.ts"
      via: "useWizardStore for sibling counts and siblingTypeExpanded toggle"
      pattern: "useWizardStore"
    - from: "src/components/wizard/WizardShell.tsx"
      to: "src/components/wizard/StepRelationship.tsx"
      via: "conditional render when currentStep === 1"
      pattern: "StepRelationship"
    - from: "src/App.tsx"
      to: "src/components/layout/AppLayout.tsx"
      via: "wraps WizardShell in AppLayout"
      pattern: "AppLayout"
---

<objective>
Build the three wizard step components, wire them into WizardShell, and connect App.tsx to render the complete wizard as the app's entry point.

Purpose: This plan delivers the user-facing wizard experience. Each step component uses the Zustand store (Plan 01) and UI primitives (Plan 02) to collect heir data through the perspective-based entry flow with auto-includes, conditional fields, and progressive disclosure. The app becomes fully interactive.

Output: Complete working wizard with all 3 steps, wired into App.tsx, with component tests and a visual checkpoint.
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-heir-input-wizard/02-CONTEXT.md
@.planning/phases/02-heir-input-wizard/02-RESEARCH.md
@.planning/phases/02-heir-input-wizard/02-01-SUMMARY.md
@.planning/phases/02-heir-input-wizard/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/stores/wizardStore.ts:
```typescript
export const useWizardStore: UseBoundStore<StoreApi<WizardState>>
// Actions used by step components:
//   setRelationship(rel: RelationshipType): void
//   setUserGender(gender: 'male' | 'female'): void
//   setMotherAlive(alive: boolean): void
//   setDeceasedGender(gender: 'male' | 'female'): void
//   setMfloEnabled(enabled: boolean): void
//   setWifeCount(n: number): void
//   setHusbandPresent(b: boolean): void
//   setSonCount(n: number): void
//   setDaughterCount(n: number): void
//   setBrother{Full|Consanguine|Uterine}Count(n: number): void
//   setSister{Full|Consanguine|Uterine}Count(n: number): void
//   setSiblingTypeExpanded(expanded: boolean): void
// State selectors:
//   relationship, deceasedGender, userGender, motherAlive, mfloEnabled
//   wifeCount, husbandPresent, sonCount, daughterCount
//   brotherFullCount, brotherConsanguineCount, brotherUterineCount
//   sisterFullCount, sisterConsanguineCount, sisterUterineCount
//   siblingTypeExpanded, currentStep, completedSteps
```

From src/types/wizard.ts:
```typescript
export type RelationshipType = 'father' | 'mother' | 'husband' | 'wife' | 'brother' | 'sister' | 'other'
```

<!-- From Plan 02 -->
From src/components/ui/StepperButton.tsx:
```typescript
export function StepperButton(props: {
  label: string, value: number, min?: number, max?: number,
  onChange: (value: number) => void, tooltip?: string, disabled?: boolean
}): JSX.Element
```

From src/components/ui/Tooltip.tsx:
```typescript
export function Tooltip(props: { content: string }): JSX.Element
```

From src/components/ui/Button.tsx:
```typescript
export function Button(props: {
  children: React.ReactNode, variant?: 'primary' | 'secondary' | 'ghost',
  onClick?: () => void, disabled?: boolean, fullWidth?: boolean, className?: string
}): JSX.Element
```

From src/components/layout/AppLayout.tsx:
```typescript
export function AppLayout(props: { children: React.ReactNode }): JSX.Element
```

From src/components/wizard/WizardShell.tsx:
```typescript
export function WizardShell(): JSX.Element
// Currently renders placeholder divs for each step -- will be updated to render actual step components
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StepRelationship, StepFamily, and StepSiblings components</name>
  <files>src/components/wizard/StepRelationship.tsx, src/components/wizard/StepFamily.tsx, src/components/wizard/StepSiblings.tsx</files>
  <action>
    **StepRelationship.tsx** (Step 1):
    - Reads from useWizardStore: relationship, userGender, motherAlive, mfloEnabled, deceasedGender + their setters
    - Section heading: "I am calculating inheritance for my..." (text-lg font-semibold text-gray-800, mb-4)
    - Relationship selector: Grid of 7 buttons (3 cols on mobile, 4 on desktop). Each button shows the relationship label. Selected button: bg-emerald-600 text-white. Unselected: bg-white border border-gray-200 text-gray-700 hover:border-emerald-300. Layout: `grid grid-cols-3 md:grid-cols-4 gap-3`
    - Labels: Father, Mother, Husband, Wife, Brother, Sister, Other
    - Gender disambiguation (shown ONLY when relationship is 'father' or 'mother'):
      - Text: "I am the deceased's..." with two buttons: "Son" and "Daughter" (same style as relationship buttons but smaller)
      - When user selects son/daughter, call setUserGender('male'/'female')
    - Mother-alive prompt (shown ONLY when relationship is 'father'):
      - Text: "Is the deceased's wife (your mother) alive?"
      - Two buttons: "Yes" and "No" styled as toggle buttons
      - When "Yes": call setMotherAlive(true). When "No": call setMotherAlive(false)
    - "Other" flow: When 'other' selected, show a deceased gender selector: "The deceased was..." with "Male" and "Female" buttons. Calls setDeceasedGender().
    - MFLO toggle: Below a divider line, under a collapsible "Advanced" section (default collapsed, toggle text: "Advanced options"). When expanded:
      - Toggle switch (styled as a pill toggle with emerald-600 when active, gray-200 when inactive)
      - Label: "Apply MFLO Section 4 (orphaned grandchildren)"
      - Tooltip: (?) with explanation "The Muslim Family Laws Ordinance 1961 Section 4 allows orphaned grandchildren to inherit their parent's share. This diverges from traditional Hanafi jurisprudence."
      - When toggled on, show inline warning banner: amber/gold bg-gold-50 border-gold-500 text with warning icon: "MFLO Section 4 modifies traditional Hanafi inheritance rules. Consult a qualified scholar for guidance."

    **StepFamily.tsx** (Step 2):
    - Reads from useWizardStore: deceasedGender, wifeCount, husbandPresent, sonCount, daughterCount + their setters
    - Also reads autoIncludes to show which heirs were auto-added (display "Auto-added" badge next to auto-included heirs)
    - Section heading: "Immediate Family" (text-lg font-semibold text-gray-800, mb-2)
    - Subtitle: "Enter the deceased's spouse and children" (text-sm text-gray-500, mb-6)
    - Spouse section (conditional on deceasedGender per CONTEXT.md):
      - If deceasedGender is 'male': StepperButton with label="Wives", min=0, max=4, value=wifeCount, onChange=setWifeCount
      - If deceasedGender is 'female': Checkbox/toggle "Was she married?" -- when checked, husbandPresent=true (husband count is always 1)
      - If deceasedGender is null (shouldn't happen if step 1 is valid, but handle gracefully): show "Please select relationship first"
    - Children section:
      - StepperButton label="Sons", value=sonCount, onChange=setSonCount
      - StepperButton label="Daughters", value=daughterCount, onChange=setDaughterCount
    - If auto-includes exist (e.g., user auto-added as son), show a subtle tag next to the relevant stepper: "(includes you)" in text-xs text-emerald-600

    **StepSiblings.tsx** (Step 3):
    - Reads from useWizardStore: all sibling counts, siblingTypeExpanded + their setters
    - Section heading: "Siblings" (text-lg font-semibold text-gray-800, mb-2)
    - Subtitle: "Enter the deceased's brothers and sisters" (text-sm text-gray-500, mb-6)
    - Default view (siblingTypeExpanded=false):
      - StepperButton label="Brothers", value = sum of all brother counts (brotherFullCount + brotherConsanguineCount + brotherUterineCount)
        - onChange: when collapsed, changes go to brotherFullCount only (default to full per CONTEXT.md)
      - StepperButton label="Sisters", value = sum of all sister counts
        - onChange: when collapsed, changes go to sisterFullCount only
    - Toggle: "Different types of siblings?" clickable text (text-sm text-emerald-600 hover:text-emerald-700 cursor-pointer) with a Tooltip explaining "In Islamic inheritance, siblings have different shares based on whether they share both parents (full), only the father (consanguine/paternal), or only the mother (uterine/maternal)."
    - Expanded view (siblingTypeExpanded=true): Replace the two simple steppers with 6 sub-type steppers, grouped:
      - "Brothers" sub-heading (text-sm font-medium text-gray-600 mb-2 mt-4)
        - StepperButton label="Full brothers", value=brotherFullCount, tooltip="Share both parents with the deceased"
        - StepperButton label="Consanguine brothers", value=brotherConsanguineCount, tooltip="Share only the father (paternal half-brothers)"
        - StepperButton label="Uterine brothers", value=brotherUterineCount, tooltip="Share only the mother (maternal half-brothers)"
      - "Sisters" sub-heading
        - StepperButton label="Full sisters", value=sisterFullCount, tooltip="Share both parents with the deceased"
        - StepperButton label="Consanguine sisters", value=sisterConsanguineCount, tooltip="Share only the father (paternal half-sisters)"
        - StepperButton label="Uterine sisters", value=sisterUterineCount, tooltip="Share only the mother (maternal half-sisters)"
    - Per RESEARCH.md Pitfall 3: toggling expanded=false does NOT reset sub-type counts. The collapsed view shows the sum but individual counts remain in the store.
    - When collapsing: if user had entered sub-types (e.g., 1 full + 1 consanguine brother), the collapsed "Brothers" stepper shows "2". Incrementing from collapsed state adds to brotherFullCount only.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && bun run build</automated>
  </verify>
  <done>All 3 step components render their respective form fields, use StepperButton and Tooltip from Plan 02, read/write Zustand store, handle auto-includes display, progressive disclosure works</done>
</task>

<task type="auto">
  <name>Task 2: Wire step components into WizardShell and App.tsx, add component tests</name>
  <files>src/components/wizard/WizardShell.tsx, src/App.tsx, src/components/__tests__/wizard.test.tsx</files>
  <action>
    **Update WizardShell.tsx:**
    - Replace placeholder step divs with actual step components:
      - `{currentStep === 1 && <StepRelationship />}`
      - `{currentStep === 2 && <StepFamily />}`
      - `{currentStep === 3 && <StepSiblings />}`
    - Import StepRelationship, StepFamily, StepSiblings from their respective files
    - Ensure AnimatePresence wraps the step components correctly (motion.div key={currentStep})

    **Update App.tsx:**
    - Replace current placeholder content with: `<AppLayout><WizardShell /></AppLayout>`
    - Import AppLayout from `@/components/layout/AppLayout`
    - Import WizardShell from `@/components/wizard/WizardShell`
    - Remove old placeholder JSX entirely. The wizard IS the app per CONTEXT.md "No landing page"

    **Create src/components/__tests__/wizard.test.tsx:**
    - Import { render, screen, fireEvent } from @testing-library/react
    - Import App from `@/App`
    - Test "renders wizard on load":
      - render(<App />)
      - Expect "Jomi-Bhag" heading visible
      - Expect "Islamic Inheritance Calculator" subtitle visible
      - Expect "I am calculating inheritance for my..." text visible (Step 1 default)
    - Test "step navigation":
      - render(<App />)
      - Select "Father" relationship button
      - Select user gender (Son/Daughter)
      - Click "Next" button
      - Expect Step 2 content visible (e.g., "Immediate Family" heading or "Sons" stepper)
      - Click "Next" again
      - Expect Step 3 content visible (e.g., "Siblings" heading)
      - Click "Back"
      - Expect Step 2 content visible again
    - Test "DSGN-03 wizard flow":
      - Verify all 3 steps are reachable through Next navigation
    - Test "HEIR-05 parents excluded":
      - Verify no "Father" or "Mother" heir entry fields exist in any step (only "Father" as relationship option, not as heir)
    - Use vitest + @testing-library/react (already in devDependencies) + jsdom environment (already configured)
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && bun vitest run src/components/__tests__/wizard.test.tsx</automated>
  </verify>
  <done>WizardShell renders actual step components with animations, App.tsx shows wizard immediately on load, component tests pass for navigation flow and HEIR-05 parents exclusion</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete wizard on mobile and desktop</name>
  <action>
    Human verifies the complete 3-step heir input wizard with perspective-based entry, auto-includes, stepper buttons, progressive sibling disclosure, MFLO toggle, responsive layout (mobile + desktop), animated step transitions, and emerald green Islamic-accented design.

    Steps to verify:
    1. Run `cd /home/siam/Personal/jomi-bhag && bun run dev` to start dev server
    2. Open browser at the URL shown (likely http://localhost:5173)

    **Desktop verification (resize browser to 1024px+):**
    - Confirm "Jomi-Bhag" / "Islamic Inheritance Calculator" header appears
    - Confirm wizard Step 1 loads immediately (no landing page)
    - Confirm "parents have passed away" info text visible
    - Select "Father" -- confirm gender disambiguation appears ("I am the deceased's Son/Daughter")
    - Select "Son" -- confirm "Is mother alive?" prompt appears
    - Select "Yes" -- proceed to Step 2
    - Confirm Step 2 shows "Wives" stepper (deceased is male) and Sons/Daughters
    - Confirm auto-include tags visible ("includes you" on Sons, wife count includes mother)
    - Click Next to Step 3
    - Confirm Brothers/Sisters steppers visible
    - Click "Different types of siblings?" -- confirm sub-types expand
    - Enter some consanguine brothers, collapse -- confirm count preserved in collapsed view
    - Navigate back to Step 1, change relationship to "Wife" -- confirm Step 2 resets spouse to "Was she married?"
    - Confirm step indicator shows checkmarks on completed steps, green on active

    **Mobile verification (resize to 375px width or use Chrome DevTools mobile):**
    - Confirm full-width layout, no horizontal scroll
    - Confirm step indicator shrinks to compact dots
    - Confirm Next/Back buttons are full-width bottom bar
    - Confirm stepper button tap targets are large enough (44px+)
    - Confirm all content is readable and not truncated

    **Animation verification:**
    - Navigate forward and backward between steps
    - Confirm smooth slide transition (left-to-right forward, right-to-left backward)

    3. Run `bun run test:run` to confirm all tests pass
    4. Run `bun run build` to confirm clean production build
  </action>
  <verify>Human confirms wizard works on mobile (375px) and desktop (1024px+), all interactions functional</verify>
  <done>Wizard approved by user for visual design, responsive layout, and functional correctness</done>
</task>

</tasks>

<verification>
- `bun run test:run` -- all tests pass (Phase 1 engine + wizard store + component tests)
- `bun run build` -- clean TypeScript build, zero errors
- `bun run dev` -- wizard loads immediately, all 3 steps navigable
- Mobile (375px): no overflow, full-width layout, large tap targets
- Desktop (1024px+): centered card, comfortable padding, step labels visible
- Auto-includes: selecting "my Father" + "Son" + "mother alive" auto-adds son and wife
- Progressive disclosure: expand siblings, enter sub-types, collapse, counts preserved
- MFLO toggle: under Advanced section, warning banner appears when enabled
</verification>

<success_criteria>
- User can navigate through all 3 wizard steps with animated transitions
- Relationship selection correctly derives deceased gender for all 7 options
- Auto-include logic works: "my Father" adds user as son/daughter and optionally mother as wife
- Stepper buttons enforce min/max (wives: 0-4, others: 0+)
- Sibling progressive disclosure works without data loss
- MFLO toggle under Advanced with warning banner
- Responsive: works at 375px mobile and 1024px+ desktop
- All automated tests pass
- Clean production build
</success_criteria>

<output>
After completion, create `.planning/phases/02-heir-input-wizard/02-03-SUMMARY.md`
</output>

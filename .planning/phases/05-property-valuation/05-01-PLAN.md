---
phase: 05-property-valuation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/mouza-rates.ts
  - src/data/__tests__/mouza-rates.test.ts
  - src/core/land/types.ts
  - src/core/land/valuation.ts
  - src/core/land/__tests__/valuation.test.ts
  - src/stores/wizardStore.ts
  - src/data/bd-land-data.ts
  - src/components/property/LandAreaInput.tsx
  - src/components/property/MouzaRateSuggestion.tsx
  - src/components/property/PropertyCard.tsx
  - src/components/__tests__/property.test.tsx
autonomous: true
requirements:
  - VALP-01
  - VALP-02

must_haves:
  truths:
    - "Rate suggestion appears below land value input when division + upazila + property type are set and rate data exists"
    - "Rate suggestion shows transparent math: BDT/decimal x area decimal = total"
    - "User can click 'Use this rate' to auto-fill land value and set rateSource to govt"
    - "User can ignore suggestion and type their own value (rateSource stays manual)"
    - "Rate suggestion does NOT appear for mixed property type"
    - "Rate suggestion does NOT appear when mouza data unavailable for selected location"
    - "Upazila dropdown resets to null when division changes"
  artifacts:
    - path: "src/data/mouza-rates.ts"
      provides: "Upazila lists for 8 division HQ districts and BDT-per-decimal rate lookup"
      exports: ["UPAZILA_BY_DIVISION", "getMouzaRate", "UpazilaInfo", "MouzaRate"]
    - path: "src/core/land/valuation.ts"
      provides: "Estate breakdown computation helpers"
      exports: ["computeEstateBreakdown", "EstateBreakdown"]
    - path: "src/core/land/types.ts"
      provides: "Property type extended with upazila and rateSource fields"
      contains: "upazila: string | null"
    - path: "src/components/property/MouzaRateSuggestion.tsx"
      provides: "Inline rate suggestion component with Use this rate button"
      exports: ["MouzaRateSuggestion"]
  key_links:
    - from: "src/components/property/LandAreaInput.tsx"
      to: "src/data/mouza-rates.ts"
      via: "getMouzaRate(division, upazila, propertyType) call"
      pattern: "getMouzaRate"
    - from: "src/components/property/LandAreaInput.tsx"
      to: "src/components/property/MouzaRateSuggestion.tsx"
      via: "conditional render when rate is non-null and area > 0"
      pattern: "MouzaRateSuggestion"
    - from: "src/stores/wizardStore.ts"
      to: "src/core/land/types.ts"
      via: "addProperty creates Property with upazila: null, rateSource: manual"
      pattern: "upazila.*null.*rateSource.*manual"
---

<objective>
Mouza rate data, property type extension, upazila dropdown, and rate suggestion UI

Purpose: Enable auto-suggestion of BD government mouza rates by district/upazila in the property input form, letting users see official land prices and optionally apply them (VALP-01), while preserving full manual override capability (VALP-02).

Output: Mouza rate data module with ~80 upazila entries across 8 divisions, extended Property type with upazila/rateSource fields, upazila cascade dropdown in LandAreaInput, inline rate suggestion component, and estate breakdown computation helper (used by Plan 02).
</objective>

<execution_context>
@/home/siam/.claude/get-shit-done/workflows/execute-plan.md
@/home/siam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-property-valuation/05-CONTEXT.md
@.planning/phases/05-property-valuation/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/core/land/types.ts:
```typescript
export type PropertyType = 'agricultural' | 'residential' | 'commercial' | 'mixed'
export type LandUnit = 'decimal' | 'katha' | 'bigha' | 'sqft'
export type Division = 'dhaka' | 'chittagong' | 'rajshahi' | 'khulna' | 'barisal' | 'sylhet' | 'rangpur' | 'mymensingh'

export interface Property {
  id: string
  nickname: string
  type: PropertyType | null
  division: Division | null
  landAreaSqft: number
  landInputUnit: LandUnit
  landValue: number
  house: HouseDetail | null
  trees: TreeDetail | null
  pond: PondDetail | null
}

export function computePropertyTotal(property: Property): number
```

From src/core/land/units.ts:
```typescript
export function toSqft(value: number, unit: LandUnit, division: Division): number
export function fromSqft(sqft: number, unit: LandUnit, division: Division): number
```

From src/data/bd-land-data.ts:
```typescript
export const BD_DIVISIONS = [
  { value: 'dhaka' as Division, label: 'Dhaka', bangla: '...' },
  // ... 8 entries
] as const
```

From src/stores/wizardStore.ts:
```typescript
// Actions relevant to this plan:
updateProperty: (id: string, patch: Partial<Property>) => void
addProperty: () => string  // creates Property with defaults
getAllPropertiesTotal: () => number
```

From src/components/property/LandAreaInput.tsx:
```typescript
interface LandAreaInputProps { propertyId: string }
// Uses: division dropdown, area input, unit selector, PropertyValueInput for land value
// Reads property from store via useWizardStore
```

From src/components/property/PropertyCard.tsx:
```typescript
function getAutoLabel(type: PropertyType | null, index: number): string
// Uses BD_DIVISIONS for division label display
// Shows property type, division, land area, and total value
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mouza rate data module, Property type extension, and valuation helper</name>
  <files>
    src/data/mouza-rates.ts,
    src/data/__tests__/mouza-rates.test.ts,
    src/core/land/types.ts,
    src/core/land/valuation.ts,
    src/core/land/__tests__/valuation.test.ts,
    src/stores/wizardStore.ts
  </files>
  <behavior>
    - getMouzaRate('dhaka', 'savar', 'residential') returns a positive number (BDT per decimal)
    - getMouzaRate('dhaka', 'savar', 'agricultural') returns a lower number than residential
    - getMouzaRate('dhaka', 'savar', 'commercial') returns a higher number than residential
    - getMouzaRate('dhaka', 'savar', 'mixed') returns null (no rate for mixed)
    - getMouzaRate('dhaka', 'nonexistent_upazila', 'residential') returns null
    - UPAZILA_BY_DIVISION['dhaka'] returns array of 5+ UpazilaInfo entries (incl city corp)
    - UPAZILA_BY_DIVISION covers all 8 divisions with realistic upazila counts
    - computeEstateBreakdown([property]) returns { land, structures, trees, ponds, total, byProperty }
    - computeEstateBreakdown correctly sums across multiple properties
    - computeEstateBreakdown handles properties with null house/trees/pond (zeros)
    - computeEstateBreakdown handles itemized vs non-itemized trees correctly
    - Property type has upazila: string | null and rateSource: 'govt' | 'manual' fields
    - addProperty() creates property with upazila: null, rateSource: 'manual'
  </behavior>
  <action>
    1. **Extend Property type** in `src/core/land/types.ts`: Add `upazila: string | null` and `rateSource: 'govt' | 'manual'` fields to the Property interface. Update computePropertyTotal signature unchanged (it doesn't use these fields).

    2. **Update wizardStore.ts** `addProperty()`: Add `upazila: null` and `rateSource: 'manual'` to the default Property object.

    3. **Create `src/data/mouza-rates.ts`**:
       - Export `UpazilaInfo` interface: `{ value: string, label: string, bangla: string }`
       - Export `MouzaRate` interface: `{ agricultural: number, residential: number, commercial: number }` (BDT per decimal)
       - Export `UPAZILA_BY_DIVISION: Record<Division, UpazilaInfo[]>` covering all 8 division HQ districts with realistic upazila lists. Include a city corporation entry for major cities (Dhaka, Chittagong, Rajshahi, Khulna, Sylhet). Use the research data for upazila names.
       - Export `getMouzaRate(division: Division, upazila: string, propertyType: PropertyType): number | null` -- returns null for 'mixed' type, null for unknown upazila, and the BDT/decimal rate otherwise.
       - Rate values: Use realistic representative government minimum rates. Dhaka area: agricultural 80K-300K, residential 500K-5M, commercial 1M-10M per decimal. Other divisions proportionally lower. Agricultural < Residential < Commercial in all areas.
       - Total ~80-90 upazila entries across all 8 divisions.

    4. **Create `src/core/land/valuation.ts`**:
       - Export `EstateBreakdown` interface with `{ land, structures, trees, ponds, total, byProperty: Array<{ property, land, structures, trees, ponds, total }> }`
       - Export `computeEstateBreakdown(properties: Property[]): EstateBreakdown` -- iterates properties, extracts land/structures/trees/ponds per property using same logic as computePropertyTotal, returns category totals and per-property breakdown.
       - Handle itemized vs non-itemized trees (same as computePropertyTotal does).

    5. **Write tests first** (TDD):
       - `src/data/__tests__/mouza-rates.test.ts`: Test getMouzaRate for valid lookups, mixed type returning null, unknown upazila returning null, all 8 divisions having entries, rate ordering (agri < resi < commercial).
       - `src/core/land/__tests__/valuation.test.ts`: Test computeEstateBreakdown with single property, multiple properties, properties with null sub-sections, itemized trees.
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && npx vitest run src/data/__tests__/mouza-rates.test.ts src/core/land/__tests__/valuation.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - getMouzaRate returns correct BDT/decimal for all 8 divisions with multiple upazilas each
    - getMouzaRate returns null for mixed type and unknown upazilas
    - computeEstateBreakdown correctly computes category totals and per-property breakdown
    - Property type has upazila and rateSource fields
    - addProperty creates property with upazila: null, rateSource: 'manual'
    - All existing tests still pass (Property shape change is backward-compatible with null defaults)
  </done>
</task>

<task type="auto">
  <name>Task 2: Upazila dropdown, rate suggestion UI, and property form integration</name>
  <files>
    src/components/property/LandAreaInput.tsx,
    src/components/property/MouzaRateSuggestion.tsx,
    src/components/property/PropertyCard.tsx,
    src/data/bd-land-data.ts,
    src/components/__tests__/property.test.tsx
  </files>
  <action>
    1. **Create `src/components/property/MouzaRateSuggestion.tsx`**:
       - Props: `{ ratePerDecimal: number, areaInDecimals: number, onApply: (totalValue: number) => void }`
       - Displays: "Govt rate: BDT X/decimal x Y.YY decimal = BDT Z total" using `Intl.NumberFormat('en-IN')` for formatting
       - "Use this rate" button calls `onApply(Math.round(ratePerDecimal * areaInDecimals))`
       - Styled: emerald-50 bg, emerald-200 border, rounded-lg, text-sm
       - Only renders when areaInDecimals > 0 (caller is responsible for rate existence check)

    2. **Add upazila dropdown to `LandAreaInput.tsx`**:
       - Import `UPAZILA_BY_DIVISION` from `src/data/mouza-rates.ts`
       - Import `getMouzaRate` from `src/data/mouza-rates.ts`
       - Import `fromSqft` from `src/core/land/units.ts` (already imported)
       - After the Division dropdown, add a conditional Upazila dropdown that only shows when division is selected
       - Upazila dropdown populated from `UPAZILA_BY_DIVISION[division]`
       - On upazila change: `updateProperty(propertyId, { upazila: newUpazila })`
       - **On division change**: extend existing `handleDivisionChange` to also clear upazila and reset rateSource: `updateProperty(propertyId, { division: newDiv, landAreaSqft: newSqft, upazila: null, rateSource: 'manual' })`

    3. **Add rate suggestion below land value input in `LandAreaInput.tsx`**:
       - Read property's `type` from store (need to access the property's type field -- already have `property` object)
       - Compute rate: `const rate = division && property.upazila && property.type && property.type !== 'mixed' ? getMouzaRate(division, property.upazila, property.type) : null`
       - Compute area in decimals: `const areaInDecimals = landAreaSqft > 0 && division ? fromSqft(landAreaSqft, 'decimal', division) : 0`
       - Render `<MouzaRateSuggestion>` below `<PropertyValueInput>` when `rate !== null && areaInDecimals > 0`
       - onApply handler: `updateProperty(propertyId, { landValue: totalValue, rateSource: 'govt' })`
       - When user manually changes land value (existing handleLandValueChange), set rateSource to 'manual': extend to `updateProperty(propertyId, { landValue: val, rateSource: 'manual' })`
       - When rate data unavailable and division + upazila selected: show subtle info text "Govt rates not available for this area -- enter your estimated land value" (gray-400 text-xs)

    4. **Update `PropertyCard.tsx`**:
       - Show "Govt rate" green badge or "Manual" gray badge next to the land value display, reading `property.rateSource`
       - Green badge: `bg-emerald-50 text-emerald-700 text-xs px-1.5 py-0.5 rounded-full`
       - Gray badge: `bg-gray-100 text-gray-500 text-xs px-1.5 py-0.5 rounded-full`
       - Only show badge when property has a landValue > 0
       - Show upazila label in the property card header/summary line next to division (e.g., "Dhaka, Savar")

    5. **Update `src/data/bd-land-data.ts`** if needed -- no changes expected since upazila data lives in mouza-rates.ts.

    6. **Add integration tests to `src/components/__tests__/property.test.tsx`**:
       - Test: Upazila dropdown appears when division selected
       - Test: Upazila dropdown options match UPAZILA_BY_DIVISION for selected division
       - Test: Rate suggestion appears when division + upazila + type set and rate exists
       - Test: Rate suggestion hidden for mixed type
       - Test: "Use this rate" button fills land value
       - Test: Govt rate / Manual badge shows correctly
  </action>
  <verify>
    <automated>cd /home/siam/Personal/jomi-bhag && npx vitest run src/components/__tests__/property.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - Upazila dropdown appears after division selection, populated with correct upazilas
    - Upazila resets to null when division changes
    - Rate suggestion shows "BDT X/decimal x Y decimal = BDT Z total" when all conditions met
    - "Use this rate" fills land value and sets rateSource to 'govt'
    - Manual value entry sets rateSource to 'manual'
    - Rate suggestion hidden for mixed type and unavailable locations
    - Govt rate / Manual badge visible on PropertyCard
    - All existing property tests still pass
  </done>
</task>

</tasks>

<verification>
1. `cd /home/siam/Personal/jomi-bhag && npx vitest run --reporter=verbose` -- full test suite green
2. `cd /home/siam/Personal/jomi-bhag && npx tsc --noEmit` -- no TypeScript errors
3. Manual spot check: select Dhaka division -> Savar upazila -> Residential type -> enter 5 decimal area -> rate suggestion shows with correct math -> click "Use this rate" -> land value auto-fills
</verification>

<success_criteria>
- getMouzaRate returns BDT/decimal rates for all 8 BD division HQ districts at upazila level
- Rate suggestion UI appears inline below land value when conditions met (division + upazila + type + rate data exists)
- User can apply suggested rate or type their own value
- rateSource field tracks whether value came from govt rate or manual entry
- Upazila dropdown cascades from division, resets on division change
- computeEstateBreakdown helper ready for Plan 02 consumption
- All tests pass including new mouza rate and property integration tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-property-valuation/05-01-SUMMARY.md`
</output>
